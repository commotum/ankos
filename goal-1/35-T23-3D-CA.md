# 35-T23-3D-CA

Status: **IN PROGRESS — SOURCE, ASSET, SEMANTIC, AND ARCHITECTURE AUDITS OPEN**

## Current Facts

- T23 is CSV line 24, `Three-Dimensional Cellular Automata`. The catalog name is search vocabulary, not a runtime family.
- DOMAIN is expected to be discrete `t+3D`. Cubic `Z^3` support/topology and its invariants belong to CONFIGURATION; finite boxes, periodic quotients, fixed exteriors, sparse representations, projections, slices, and volume renderings remain explicit realizations or views.
- T21/D127 and T22/D128 already establish one branch-free fixed-lattice SimpleProgram runner with explicit Self access, ordered offsets, complete or factorized closed RULE data, same-site writes, and old-snapshot parallel UPDATE. T23 must prove whether dimension/access/rule data suffice at one native event per event; general CA encodability is not evidence.
- Primary Chapter 5 candidates distinguish face-adjacent cubic access from the full `3x3x3` cube. These must remain typed access/case descriptors rather than being collapsed by a picture, count, or current runtime family name.
- The Notes' `AxesTotal` and `FullTotal` profiles suggest dimension-parameterized sum/product case maps. Exact table sizes, code direction, positional order, symmetry restrictions, seeds, boundaries, and named examples remain evidence obligations until the source audit closes.
- The current `dyadaxes_3d` neighborhood exposes Self, six face neighbors, and twenty edge/corner neighbors, but its RULE majority/threshold gates discard raw values. Shared geometry does not make that lossy RULE a general T23 preset.
- Three-dimensional visualization can reverse axes, project depth, hide cells, or crop a finite box. Those mappings are representation metadata and cannot define native coordinates, support, transition values, or rule identity.
- The governing event remains:

  ```text
  active = FRONTIER.select(configuration)
  reads  = NEIGHBORHOOD.read(configuration, active)
  writes = RULE(active, reads)
  next   = UPDATE.apply(configuration, active, writes)
  ```

## Updated Assumptions

- Strict T23 uses a fixed cubic `Z^3` support. “Three-dimensional” does not silently include the other 3D lattices, tilings, or nearest-neighbor incidence structures routed to T24; dimension, support topology, and access are separate declarations.
- The four main Chapter 5 predicates depend only on the six- or twenty-six-position surrounding shell. They may be represented either by a shell-count table or, losslessly, by the Notes' broader `SelfValue x ShellValueSum` table with an `IgnoreSelf` fiber invariant. The two schemas must retain their distinct identities.
- The Notes' `AxesTotal` and `FullTotal` constructions do read the old center as a separately weighted factor. Their strict binary three-dimensional case counts are respectively fourteen and fifty-four; this is explicit RULE data, not an executor mode or a hidden Self read.
- A general positional rule uses the complete sorted offset tuple and the complete context. A count rule cannot certify positional order, and neither a symmetric fixture nor a rendered projection may be used as the orientation oracle.
- `Cuboid[-Reverse[position]]`, top views, depth shading, cropping, and volume display are observer mappings. Raw Book array offsets plus a frame tag are primary; any Book-frame-to-ENU isomorphism is a separately declared representation that must carry the whole positional table.
- The three Carter Bays triples are named values of the same center-plus-26-count schema. Their moving structure is a trajectory/observer fixture, not a new Life-like engine.
- No primary evidence presently changes the fixed-support, all-sites, same-site-write, old-snapshot parallel event. A distinct T23 state class, UPDATE, or executor remains unjustified unless the closed audits produce a counterexample.

## Big Picture Objective

Reconstruct three-dimensional cellular automata from primary evidence and determine the smallest faithful reuse of the SimpleProgram axes. Produce an implementation-ready Goal 2 handoff without a `ThreeDimensionalState`, 3D executor, family branch, hidden center/boundary, visualization-defined rule, or Dyadaxes substitution.

## Catalog Identity

- Stable ID: T23.
- Exact catalog name: Three-Dimensional Cellular Automata.
- CSV line: 24.
- Taxonomy section: 23.
- Entry kind: fixed-support synchronous local transition construction; neighborhood/dimensional/RULE parameterization is the hypothesis to test.
- Initial vocabulary: three-dimensional/3D cellular automata, cubic lattice, cube, face neighbors, edge neighbors, corner neighbors, six neighbors, twenty-six neighbors, `AxesTotal`, `FullTotal`, projections, slices, volumes, growth, polyhedra, spheres, class 4, Notes implementation, actual Index.

## First-Principles Fit Standard

The `src/ca` namespace and its Phase 1 rank-three implementation are not the semantic boundary. T23 fits the existing construction only if its complete configuration and one native event commute one-for-one through the shared axes without a hidden interpreter, microsteps, family dispatch, alternate trace clock, or opaque whole-state packing.

Every finding must be classified as:

1. direct reuse of an existing construction;
2. a parameterization, restriction, preset, invariant, or named role;
3. a lossless tagged/product/coordinate/table representation of an existing construction;
4. or a genuinely different execution algebra justified by a concrete one-step counterexample.

## Initial Runtime/API Fit Audit

- `src/ca/loci.py:1-74` already uses canonical `[t,x,y,z]` coordinates and admits spatial ranks zero through three. Its centered finite tensors are realization coordinates, not the native cubic support itself.
- `src/ca/neighborhoods.py:572-620` parameterizes both `moore(axes=("x","y","z"))` and `von_neumann(axes=("x","y","z"))`. At radius one these give the twenty-six-cell surrounding cube and six face-adjacent cells. Composing either with `self_at()` preserves the source's separate center role without a T23 neighborhood class.
- `src/ca/neighborhoods.py:743-764` exposes Self, the same six faces, and the remaining twenty edge/corner positions as three raw components. `src/ca/rules.py:495-515` then reduces the six-face count to strict majority and the twenty-position count to `>=10` before an eight-row lookup. That RULE is lossy for exact neighbor-count or positional T23 rules even though its access geometry covers the full cube.
- `src/ca/rollout.py:146-213` mechanically rolls rank-three arrays from old snapshots but selects entry through named-family branches. The shared spatial kernel is a candidate lowering; `dyadaxes_3d` dispatch is not the SimpleProgram abstraction.
- The exhaustive channel uses the first gathered slot as the least-significant digit and `numpy.int64` (`src/ca/rollout.py:742-759`). The Book uses lexicographically sorted offsets with its declared significance convention. T23 therefore inherits the shared orientation/table-permutation and arbitrary-precision identity obligations rather than receiving a 3D codec shim.
- `src/ca/specs.py:95-179` resolves only six Phase 1 family names, while `datasets.py:110-124` fixes the 3D Dyadaxes experiment to a `5x5x5` tensor, selected seed families, and fixed-zero boundary. These are current realization/experiment limits, not evidence for a T23 family or native finite cube.
- `BOOK:13509-13511` maps native array positions through `Cuboid[-Reverse[position]]` for display. That negative reversal is view metadata and cannot silently mutate the program's coordinate frame or positional table.

| Current area | Initial fit | Audit obligation |
|---|---|---|
| `simple_programs.md` | DIRECT/PARAMETERIZATION for its documented rank-three fixed-tensor scope | retain the fixed-lattice preset but keep native support, boundary realization, access, RULE schema, and UPDATE on their broad axes |
| `loci.py` | DIRECT finite realization and coordinate-frame primitive | certify Book-to-runtime coordinate/table mapping; do not identify finite array order with native `Z^3` |
| `frontiers.py` | DIRECT finite `AllSites` lowering through `time_slice(shape)` | no `ThreeDimensionalFrontier`; semantic firing is one occurrence per old site |
| `neighborhoods.py` | DIRECT/PARAMETERIZATION for Self, six-face, and twenty-six-shell access | validate exact source order, uniqueness, dimensionality, and small-quotient slot multiplicity |
| `rules.py` | PARTIAL: closed channels/aggregates exist; Dyadaxes gates are lossy | add only source-backed case/restriction descriptors, complete tables, codecs, and validators |
| `rollout.py` | MECHANICAL reuse behind family dispatch, significance, and fixed-width defects | lower the branch-free runner through the shared kernel; no 3D branch |
| `specs.py` / `datasets.py` | CONTROL/realization only | decode structural axes and preserve experiment identity separately |
| current tests | PARTIAL Phase 1 experiment coverage | add source predicates/traces, asymmetric orientation, count-fiber rejection, aliases, boundaries, and same-runner conformance |

## Initial Primary Evidence

### Cubic configuration and direct six-face examples

- Sources: `BOOK:2156,2236-2256`.
- Establishes: the source's strict 3D arrangement is a cubic lattice; the two direct face-neighbor examples output black for respectively any black face neighbor and exactly one black face neighbor. Both use a single-black-cell initial condition. Octahedral limit shape and nested appearance are run observations.

### Full surrounding-cube examples

- Source: `BOOK:2262`.
- Establishes: a second access profile uses all 26 positions sharing a face, edge, or corner with the center and outputs black for exactly one or exactly two black surrounding cells. The single cell and line-of-three are independent seeds.

### Dimension-parameterized compact tables

- Sources: `BOOK:13483-13507`.
- Establishes: in dimension `d` and alphabet size `k`, the face profile uses `self + k*AxesTotal` with table length `k*(2*d*(k-1)+1)`, while the full cube uses `self + k*FullTotal` with length `k*((3^d-1)*(k-1)+1)`. For strict binary `d=3`, these are 14- and 54-case complete product tables. Treating the center as separate is source mechanics, not a hidden read.

### General positional order and display mapping

- Sources: `BOOK:13509-13518`.
- Establishes: arbitrary local offsets in any dimension are sorted lexicographically before context digits are formed. Separately, `Graphics3D` displays black cells as `Cuboid[-Reverse[position]]`. The first fact defines program/table order; the second defines a view mapping.

### Projection evidence remains a view

- Source: `BOOK:13632-13640`.
- Establishes: four thirty-step results are shown from above with nearer cells darker, including exact-three-of-26 variants and different initial blocks. Projection depth/tone does not enter their update rule.

### Named 3D class-4 outer-totalistic presets

- Sources: `BOOK:14263-14273`.
- Establishes: the source computes count over the 26 surrounding positions and combines it with the old center. Parameter triple `(p,q,r)` preserves an old black center for counts in `[p,q]` and births at count `r`; named examples are `(5,7,6)`, `(4,5,5)`, and `(5,6,5)`. These are values/presets of a center-plus-count table unless contrary evidence appears; the moving structure is an observer/run fixture.

## Candidate Case Descriptors to Test

```text
FaceProduct[d=3,k]:
    cases = SelfValue x FaceValueSum[0..6*(k-1)]
    index = SelfValue + k*FaceValueSum
    case_count = k*(6*(k-1)+1)

FullCubeProduct[d=3,k]:
    cases = SelfValue x FullNeighborValueSum[0..26*(k-1)]
    index = SelfValue + k*FullNeighborValueSum
    case_count = k*(26*(k-1)+1)

GeneralFaceContext[k]:
    cases = Alphabet^7 in declared Self-plus-six order

GeneralCubeContext[k]:
    cases = Alphabet^27 in declared `{-1,0,1}^3` order

IgnoreSelfRestriction:
    invariant = outputs constant across Self for equal surrounding aggregate

ShellCountOnly[d=3,k]:
    cases = NeighborValueSum[0..shell_size*(k-1)]
    relation = lossless factor of the corresponding product table
               exactly on the IgnoreSelf-valid image
```

For binary strict profiles, the compact face/full product spaces contain `2^14` and `2^54` rules; the count-only main examples lie in the `2^7` and `2^27` ignore-Self restrictions. General positional tables have respectively 128 and `2^27` input contexts and therefore `2^128` and `2^(2^27)` possible binary rules. These counts are candidate derivations to verify against the complete source and an independent oracle, not new semantic classes.

## Construction Model

- **DOMAIN:** discrete `t+3D`.
- **CONFIGURATION/support:** a total finite-alphabet field on fixed cubic `Z^3` support. A finite box, periodic quotient, fixed exterior, sparse background field, causal work volume, crop, and raster/mesh are explicit realizations or views rather than the native topology.
- **ALPHABET:** binary in the direct examples; the Notes use a finite `k`-color alphabet with an explicit numeric valuation for sum-indexed compact tables.
- **Control:** none. The current label field is the complete Markov configuration.
- **FRONTIER:** every old site fires once for an ordinary event. Finite execution lowers that semantic set to the declared work realization without changing program identity.
- **NEIGHBORHOOD:** either the six distinct face offsets or all twenty-six distinct surrounding offsets, with Self composed explicitly only when the rule schema reads it. General positional rules preserve declared offset order; aggregate rules preserve read-occurrence multiplicity even when a quotient makes offsets alias one physical cell.
- **RULE:** one closed same-site finite-alphabet result. Direct predicates are shell-count tables; the Notes additionally define center-plus-shell-value-sum product tables and arbitrary complete positional maps. `IgnoreSelf`, named predicates, and Carter Bays triples are validated restrictions/presets, not evaluator branches.
- **UPDATE:** validate exactly one result for every selected source and atomically assign all results from the same old snapshot. Support and topology remain fixed.
- **Successor/outcome:** one deterministic successor for every valid event. An unchanged configuration or stopped growth is not an inferred halt.
- **Seeds/realizations:** a single cell, line of three, row of eleven, `3x1x1` block, `3x3x1` block, random field, finite array, boundary, and horizon remain independent configuration/run data at the exact source-backed boundary.
- **Observers/relations:** octahedral or nested shape, complexity class, moving structures, top projections, depth shading, slices, volumes, and display-axis transforms do not feed native evolution.

## Smallest Goal 2 Composition

```text
Domain        = DiscreteSpace(dimension=3)
Configuration = FixedLattice(OrthogonalGrid(Z^3), Alphabet)
Frontier      = AllSites
Neighborhood  = FaceShell3D | MooreShell3D
              + OptionalExplicit(SelfAt)
Rule          = OrderedContextTable
              | ProductCaseTable(SelfValue, ShellValueSum)
              | ShellAggregateTable(ShellValueSum)
              + ValidatedRestriction(ignore-self/named-predicate)
Update        = SnapshotParallelSameSite
Seed          = IndependentValidatedConfiguration
```

These names describe structural roles for the handoff; they do not mandate construction-specific public classes.

### Candidate implementation delta

1. Reuse the synthesis-selected T01/T21 fixed-lattice configuration, `AllSites`, complete old-snapshot reads, same-site finite-alphabet write, snapshot-parallel UPDATE, deterministic `StepResult`, and support/realization separation. Parameterize the declared regular support by three axes; do not add `ThreeDimensionalState`.
2. Express the six-face and twenty-six-shell profiles through ordinary explicit offsets or the generic L1/L-infinity radius constructors. A `3d` or cubic preset may return those components but may not select rollout.
3. Reuse T02's complete ordered-context map and T03's valuation/aggregate/case-table machinery. Add only any missing closed product/shell case descriptors, factor maps, and validators that are generic across dimension and alphabet.
4. Keep structural tables and schema tags primary. General seven-position and twenty-seven-position tables require respectively 128 and `2^27` context results; their codecs/lookup representations must support exact arbitrary precision or closed random access rather than narrowing program identity to `numpy.int64` or requiring eager enumeration.
5. Preserve raw sorted Book tuples with an explicit frame. Any signed axis permutation into canonical ENU must carry offsets, contexts, and the complete positional table and prove a commuting inverse. The source's `Cuboid[-Reverse[position]]` remains a separate view transform.
6. Expose the direct any/exact-one/exact-two/exact-three predicates and the three Carter Bays triples as ordinary closed presets over the relevant shell/case schema. The triple `(4,5,5)` is `B5/S45`; the other two remain equally structural birth/survival values. None owns an engine.
7. Reuse T08 configuration constructors/laws for points, lines, blocks, and random fields. Bind periodic/fixed boundaries, finite work shapes, horizons, projections, palettes, and moving-structure observations to run/realization/view records, never to the T23 program.
8. Preserve the existing rank-three tensor kernel only as a candidate finite lowering behind the branch-free runner. Remove family-name dispatch during the shared Goal 2 migration; do not route T23 through `dyadaxes_3d`.

### Required conformance areas

1. Reproduce the four direct shell predicates, their exact source seeds, and source-determined checkpoints where available; separately validate the exact-three projection variants and the three class-4 triples.
2. Prove one-step commutation between a literal Book-array evaluator and the generic SimpleProgram path for six-face, twenty-six-shell, positional, product-count, and named-preset rules.
3. Cover all 128 face-contexts directly. For the `2^27` full-cube contexts, prove every positional projection and every compact aggregate fiber combinatorially, exercise zero/full/unit bases, and reject a one-row fiber violation without pretending to enumerate the impossible rule space.
4. Prove exact expansion/factorization for qualifying compact tables and `IgnoreSelf` restrictions; reject interchange among shell-only, `Self x sum`, and positional schemas even when a small numeric spelling coincides.
5. Certify the Book-array-frame-to-ENU signed permutation and its inverse on all seven and twenty-seven positions, carry the complete table, and exhibit an asymmetric case where naive re-sorting changes the result.
6. On small periodic quotients, retain six or twenty-six read occurrences even when several resolve to one cell; reject coordinate deduplication.
7. Distinguish native `Z^3`, finite fixed-exterior work, periodic quotients, sparse background lowerings, and view/crop identities. A sparse lowering requires a quiescent-background proof.
8. Use an order-sensitive fixture to separate old-snapshot parallel UPDATE from in-place scanning and validate exact selected-source/write provenance before commit.
9. Reconstruct the Carter Bays triples as complete outer-count predicates and keep the moving structure as a trace fixture. Do not infer its untranscribed cells from the raster.
10. Give a concrete Dyadaxes collision—empty shell versus one face neighbor suffices—whose gated summary agrees but whose exact-one T23 result differs.
11. Execute T21, T22, and T23 through the identical generic step function with only dimensional, topology, access, and RULE data changed.
12. Reject missing/duplicate offsets, implicit/duplicate Self, wrong rank/order/frame, incomplete tables, invalid valuation/output, stale reads, wrong targets, duplicate source coverage, unsupported realization maps, and family dispatch.

## Audit Questions

1. Which source lines define the strict 3D configuration, firing frontier, access positions, rule cases, same-site result, and update schedule?
2. Are six-face, full-twenty-six, positional, outer-totalistic, totalistic, growth, symmetry, and named profiles restrictions or representations over one fixed-lattice event?
3. What is the exact Book coordinate/order convention, and how must any runtime axis permutation transform the complete table?
4. Which finite boxes, boundaries, backgrounds, seeds, projections, slices, and rasters are realizations or observers rather than native state?
5. Does any stochastic 3D profile require visible transition-time probability/RNG state, or is it only a relation/control for this stage?
6. Where does current `dyadaxes_3d` preserve raw geometry, and give an explicit pair of neighborhoods whose gated summary loses a T23 distinction.
7. Can T21, T22, and T23 execute through one typed runner with dimension/access/rule descriptors and no catalog branch?
8. Does any primary evidence change RULE result, UPDATE composition/schedule, successor cardinality, outcome, or configuration topology beyond fixed cubic support?

## Detailed Implementation Plan

1. Freeze a zero-remainder source query union across the monolith, Notes, actual Index, split corpus, Atlas, catalog, and taxonomy; partition native, relation, control, exclusion, and governed continuation evidence.
2. Derive the complete source-bound asset candidate set and bind every governed or adjacency-only file to source lines and physical hashes without inventing pixel or stochastic replay.
3. Build an independent native/generic semantic oracle for every source-backed strict access/RULE profile, coordinate/table permutation, finite realization boundary, alias multiplicity, and old-snapshot event.
4. Audit `simple_programs.md`, all current `src/ca` modules/tests, D004-D008, D111-D128, and the broader SimpleProgram protocol from first principles.
5. Add D129 only if the evidence establishes a new parameterization/representation boundary; no construction-named class or executor is presumed.
6. Obtain independent hostile review; run root/relocated/optimized/import/compile/Markdown/diff/scope/status/test gates; integrate the plan, evidence index, design ledger, architecture audit, and Goal 2 handoff.

## No-Cheating Checks

- No `three_dimensional`, `3d`, cubic, or T23 rollout branch or executor.
- No finite tensor, periodic boundary, crop, projection, slice, palette, or raster presented as native `Z^3` semantics.
- No hidden Self read, flattened memory-order codec, coordinate re-sort without complete table permutation, or coincident-read deduplication.
- No Dyadaxes majority/threshold summary substituted for raw face/edge/corner values or source-backed aggregates.
- No seed, horizon, shape, volume, class, growth stop, or application fed into program identity.
- No arbitrary CA encoding, callback, family dispatch, or opaque state packing accepted as native one-step reuse.

## Completion Requirements

- [ ] Exhaustive source/split/Notes/Index/alias audit closes with zero unresolved candidates.
- [ ] Source-bound asset fixed point closes with honest claim boundaries.
- [ ] Exact 3D access, RULE schemas/restrictions, coordinate mappings, and named fixtures are independently proven.
- [ ] Smallest reusable base is classified with a concrete-counterexample gate for any claimed new algebra.
- [ ] Current API/runtime/principles audit and Goal 2 handoff are implementation-ready.
- [ ] Independent hostile review and all oracle/test/Markdown/diff/scope/coverage gates pass.
- [ ] `0-plan.md`, `evidence-index.md`, `design-ledger.md`, and `architecture-audit.md` agree.

## Stage Results

In progress.
