# 35-T23-3D-CA

Status: **COMPLETE**

## Current Facts

- T23 is CSV line 24, `Three-Dimensional Cellular Automata`. The catalog name is search vocabulary, not a runtime family.
- DOMAIN is expected to be discrete `t+3D`. Cubic `Z^3` support/topology and its invariants belong to CONFIGURATION; finite boxes, periodic quotients, fixed exteriors, sparse carriers, coordinate frames, projections, slices, and volume renderings remain explicit representations, realizations, or views.
- T21/D127 and T22/D128 already establish one branch-free fixed-lattice SimpleProgram runner with explicit Self access, ordered offsets, complete or factorized closed RULE data, same-site writes, and old-snapshot parallel UPDATE. T23 must prove whether dimension/access/rule data suffice at one native event per event; general CA encodability is not evidence.
- Primary Chapter 5 candidates distinguish face-adjacent cubic access from the full `3x3x3` cube. These must remain typed access/case descriptors rather than being collapsed by a picture, count, or current runtime family name.
- The Notes' `AxesTotal` and `FullTotal` profiles suggest dimension-parameterized sum/product case maps. Exact table sizes, code direction, positional order, symmetry restrictions, seeds, boundaries, and named examples remain distinct evidence obligations; the closed audits below disposition each one.
- The current `dyadaxes_3d` neighborhood exposes Self, six face neighbors, and twenty edge/corner neighbors, but its RULE majority/threshold gates discard raw values. Shared geometry does not make that lossy RULE a general T23 preset.
- Three-dimensional visualization can reverse axes, project depth, hide cells, or crop a finite box. Those mappings are representation metadata and cannot define native coordinates, support, transition values, or rule identity.
- The governing event remains:

  ```text
  active = FRONTIER.select(configuration)
  reads  = NEIGHBORHOOD.read(configuration, active)
  writes = RULE(active, reads)
  next   = UPDATE.apply(configuration, active, writes)
  ```

## Updated Assumptions

- Strict T23 uses a fixed cubic `Z^3` support. “Three-dimensional” does not silently include the other 3D lattices, tilings, or nearest-neighbor incidence structures routed to T24; dimension, support topology, and access are separate declarations.
- The four main Chapter 5 predicates depend only on the six- or twenty-six-position surrounding shell. They may be represented either by a shell-count table or, losslessly, by the Notes' broader `SelfValue x ShellValueSum` table with an `IgnoreSelf` fiber invariant. The two schemas must retain their distinct identities.
- The Notes' `AxesTotal` and `FullTotal` constructions do read the old center as a separately weighted factor. Their strict binary three-dimensional case counts are respectively fourteen and fifty-four; this is explicit RULE data, not an executor mode or a hidden Self read.
- A general positional rule uses the complete sorted offset tuple and the complete context. A count rule cannot certify positional order, and neither a symmetric fixture nor a rendered projection may be used as the orientation oracle.
- `Cuboid[-Reverse[position]]`, top views, depth shading, cropping, and volume display are observer mappings. Raw Book array offsets plus a frame tag are primary; any Book-frame-to-ENU isomorphism is a separately declared representation that must carry the whole positional table.
- The three Carter Bays triples are named values of the same center-plus-26-count schema. Their moving structure is a trajectory/observer fixture, not a new Life-like engine.
- The source's 3D majority rule `UnitStep[a+AxesTotal[a,3]-4]` is the ordinary center-plus-six-face threshold `SelfValue + FaceCount >= 4`. P-completeness is a relation/property of that preset, not another execution mode.
- No primary evidence presently changes the fixed-support, all-sites, same-site-write, old-snapshot parallel event. A distinct T23 state class, UPDATE, or executor remains unjustified unless the closed audits produce a counterexample.

## Big Picture Objective

Reconstruct three-dimensional cellular automata from primary evidence and determine the smallest faithful reuse of the SimpleProgram axes. Produce an implementation-ready Goal 2 handoff without a `ThreeDimensionalState`, 3D executor, family branch, hidden center/boundary, visualization-defined rule, or Dyadaxes substitution.

## Catalog Identity

- Stable ID: T23.
- Exact catalog name: Three-Dimensional Cellular Automata.
- CSV line: 24.
- Taxonomy section: 23.
- Entry kind: fixed-support synchronous local transition construction; neighborhood/dimensional/RULE parameterization is the hypothesis to test.
- Initial vocabulary: three-dimensional/3D cellular automata, cubic lattice, cube, face neighbors, edge neighbors, corner neighbors, six neighbors, twenty-six neighbors, `AxesTotal`, `FullTotal`, projections, slices, volumes, growth, polyhedra, spheres, class 4, Notes implementation, actual Index.

## Search Log

`35-T23-source-oracle.py` freezes nineteen overlapping direct-name, access, Notes-implementation, positional-rule, display, class-4, majority, stochastic, realization/update, other-geometry, continuous-CA, symmetry, actual-Index, seed, restriction, and discovered localized-structure queries. Their union contains 151 unique monolith lines: 104 before the actual Index and 47 actual-Index routes. The union digest is `03268aed2534b66f807af417c006cf1a9209d195bc5d7f36d36b7a17134ae875`.

The 104 pre-Index candidates partition exactly as 72 retained matches plus 32 explicit exclusions; no candidate remains unresolved. The exclusions are `7 other SimpleProgram/dimension + 13 other CA family/observer + 7 random/statistical model + 4 other geometry/physics + 1 broad rule phrase`. The 47 actual-Index routes partition as `8 T23 + 8 T22/T24 + 17 T44 + 7 stochastic + 7 symmetry`; they provide navigation vocabulary only. The nineteenth query explicitly guards `BOOK:21473`, the discovered “localized structures … in three dimensions 949” route to the Carter Bays material.

The 72 retained matches plus 66 governed continuation lines produce 138 retained evidence lines with digest `92ce01dbf10875f7549f3eedb180a9001c72c588494247ec13d6b9f5d7160c07`. Their exact semantic partition is `76 native / 16 relation / 46 control`. Native evidence contains the cubic/access mechanics, arbitrary-dimensional compact formulas, complete positional order/codec, seed/realization mechanics, old-snapshot update, class-4 triples, and majority preset. Displays, projections, hardware, and moving-structure plates remain relations. T21/T22/T24/T44, stochastic evolution, alternate schedules, other lattices, and two-dimensional symmetry counts remain controls.

All seventeen split Markdown files are hash-bound. The query reverse join closes 151 records as `142 exact + 9 mapped variants`; the retained reverse join closes `123 exact + 15 mapped variants`, with zero monolith-only retained lines. Two Atlas hits are relation/navigation evidence. The catalog and taxonomy are checked for stable T23 identity and vocabulary but never promoted into Book semantics. The stable asset interface contains ten governed image lines with digest `321e19bd6ddda35985b08d095c182b529076ce4eea99854230c5b512b6f115ef`.

## Source-Bound Asset Audit

`35-T23-asset-oracle.py` derives an explicit one-hop universe containing every image line within four source lines of the full 138-line retained evidence set. This yields 42 physical plates and contains the ten governed source images. It is a bounded source-proximity candidate rule, not the iterative transitive closure of image adjacency; applying the rule again would define a different, larger audit universe. The declared 42-plate universe partitions without remainder into ten governed plates and 32 separately ledgered adjacency-only candidates. The governed partition is:

```text
six-face direct plates           2
full-26 direct plates            2
projection observers             4
(4,5,5) moving-structure observer 1
T24 other-lattice control        1
```

The adjacency-only partition is `11 T22 / 1 T24 / 3 random-seed / 4 stochastic-update / 1 nonstep Voronoi / 1 alternate-update / 11 relation`. Random initial configurations and transition-time stochastic rules are explicitly distinct; neither is inferred from proximity to T23. The sole nonstep plate is not used to recast T23 as a constraint system, and the sequential-update plate is a schedule counterexample rather than a variant of the strict event.

Every candidate has one monolith and one split reference, giving 84 references, 42 physical files, and 42 unique hashes. The candidate-set digest is `dcb93e36f067ee718cb7b0abf6bbd18ba42dd872b119178533e03933c014cc30`; governed and adjacency-only ledger digests are respectively `c9e97695a427f4355cd9e6edffe6ba919b55b23a195a236ae0bfceeaaca597c8` and `6d68210c6b265dcbb9fcb3ab2297501cb4a32c59b74f454f8e8707ad8204f34d`.

A nine-record transcript ledger binds the direct face/full predicates and icons, exact-one/exact-two/exact-three projection relations, source-stated seeds/horizons, and source-listed `(4,5,5)` moving-structure label to both source and asset hashes. `B5/S45` is recorded only as a derived canonical birth/survival spelling of that tuple, never as source wording. The governed T24 control is hash-bound but not transcribed into this strict ledger. Its digest is `07d1261a07ddd0f5ecb5fcf311335b2310d148174e1a09641070a6daa492deed`. The honest claim boundary is `HASH_BOUND=42 / TRANSCRIBED=9 / PIXEL_REPLAYED=0`; no missing configuration, RNG/distribution, crop, projection renderer, or voxel trace is invented. Printed-page offsets and the Notes reverse join are guarded.

## First-Principles Fit Standard

The `src/ca` namespace and its Phase 1 rank-three implementation are not the semantic boundary. T23 fits the existing construction only if its complete configuration and one native event commute one-for-one through the shared axes without a hidden interpreter, microsteps, family dispatch, alternate trace clock, or opaque whole-state packing.

Every finding must be classified as:

1. direct reuse of an existing construction;
2. a parameterization, restriction, preset, invariant, or named role;
3. a lossless tagged/product/coordinate/table representation of an existing construction;
4. or a genuinely different execution algebra justified by a concrete one-step counterexample.

## Initial Runtime/API Fit Audit

- `src/ca/loci.py:1-74` already uses canonical `[t,x,y,z]` coordinates and admits spatial ranks zero through three. Its centered finite tensors are realization coordinates, not the native cubic support itself.
- `src/ca/neighborhoods.py:572-620` parameterizes both `moore(axes=("x","y","z"))` and `von_neumann(axes=("x","y","z"))`. At radius one these give the twenty-six-cell surrounding cube and six face-adjacent cells. Composing either with `self_at()` preserves the source's separate center role without a T23 neighborhood class.
- `src/ca/neighborhoods.py:743-764` exposes Self, the same six faces, and the remaining twenty edge/corner positions as three raw components. `src/ca/rules.py:495-515` then reduces the six-face count to strict majority and the twenty-position count to `>=10` before an eight-row lookup. That RULE is lossy for exact neighbor-count or positional T23 rules even though its access geometry covers the full cube.
- `src/ca/rollout.py:146-213` selects execution through named-family branches, while `src/ca/rollout.py:576-682` contains the mechanically rank-generic prior-state-to-next-array spatial kernel. That kernel is a candidate finite lowering; `dyadaxes_3d` dispatch is not the SimpleProgram abstraction.
- The exhaustive channel uses the first gathered slot as the least-significant digit and `numpy.int64` (`src/ca/rollout.py:742-759`). The Book uses lexicographically sorted offsets with its declared significance convention. T23 therefore inherits the shared orientation/table-permutation and arbitrary-precision identity obligations rather than receiving a 3D codec shim.
- `src/ca/specs.py:95-179` resolves only six Phase 1 family names, while `datasets.py:110-124` fixes the 3D Dyadaxes experiment to a `5x5x5` tensor, selected seed families, and fixed-zero boundary. These are current realization/experiment limits, not evidence for a T23 family or native finite cube.
- `BOOK:13509-13511` maps native array positions through `Cuboid[-Reverse[position]]` for display. That negative reversal is view metadata and cannot silently mutate the program's coordinate frame or positional table.

| Current area | Initial fit | Audit obligation |
|---|---|---|
| `simple_programs.md` | DIRECT/PARAMETERIZATION for its documented rank-three fixed-tensor scope | retain the fixed-lattice preset but keep native support, boundary realization, access, RULE schema, and UPDATE on their broad axes |
| `loci.py` | DIRECT finite realization and coordinate-frame primitive | certify Book-to-runtime coordinate/table mapping; do not identify finite array order with native `Z^3` |
| `frontiers.py` | DIRECT finite `AllSites` lowering through `time_slice(shape)` | no `ThreeDimensionalFrontier`; semantic firing is one occurrence per old site |
| `neighborhoods.py` | DIRECT/PARAMETERIZATION for Self, six-face, and twenty-six-shell access | validate exact source order, uniqueness, dimensionality, and small-quotient slot multiplicity |
| `rules.py` | PARTIAL: closed channels/aggregates exist; Dyadaxes gates are lossy | add only source-backed case/restriction descriptors, complete tables, codecs, and validators |
| `rollout.py` | MECHANICAL reuse behind family dispatch, significance, and fixed-width defects | lower the branch-free runner through the shared kernel; no 3D branch |
| `specs.py` / `datasets.py` | CONTROL/realization only | decode structural axes and preserve experiment identity separately |
| current tests | PARTIAL Phase 1 experiment coverage | add source predicates/traces, asymmetric orientation, count-fiber rejection, aliases, boundaries, and same-runner conformance |

## Primary Book Evidence

### Cubic configuration and direct six-face examples

- Sources: `BOOK:2156,2236-2256`.
- Establishes: the source's strict 3D arrangement is a cubic lattice; the two direct face-neighbor examples output black for respectively any black face neighbor and exactly one black face neighbor. Both use a single-black-cell initial condition. Octahedral limit shape and nested appearance are run observations.

### Full surrounding-cube examples

- Source: `BOOK:2262`.
- Establishes: a second access profile uses all 26 surrounding positions and outputs black for exactly one or exactly two black surrounding cells. The Book literally says the cells “share either a face or a corner”; it does not say “edge.” The familiar `6 face + 12 edge + 8 corner = 26` partition is a separately labeled geometric derivation from `{-1,0,1}^3 - {(0,0,0)}`, not a repaired quotation. The single cell and line-of-three are independent seeds.

### Dimension-parameterized compact tables

- Sources: `BOOK:13483-13507`.
- Establishes: in dimension `d` and alphabet size `k`, the face profile uses `self + k*AxesTotal` with table length `k*(2*d*(k-1)+1)`, while the full cube uses `self + k*FullTotal` with length `k*((3^d-1)*(k-1)+1)`. For strict binary `d=3`, these are 14- and 54-case complete product tables. Treating the center as separate is source mechanics, not a hidden read.

### General positional order and display mapping

- Sources: `BOOK:13509-13518`.
- Establishes: arbitrary local offsets in any dimension are sorted lexicographically before context digits are formed. Separately, `Graphics3D` displays black cells as `Cuboid[-Reverse[position]]`. The first fact defines program/table order; the second defines a view mapping.

### Projection evidence remains a view

- Source: `BOOK:13632-13640`.
- Establishes: four thirty-step results are shown from above with nearer cells darker, including exact-three-of-26 variants and different initial blocks. Projection depth/tone does not enter their update rule.

### Named 3D class-4 outer-totalistic presets

- Sources: `BOOK:14263-14273`.
- Establishes: the source computes count over the 26 surrounding positions and combines it with the old center. Parameter triple `(p,q,r)` preserves an old black center for counts in `[p,q]` and births at count `r`; named examples are `(5,7,6)`, `(4,5,5)`, and `(5,6,5)`. These are values/presets of a center-plus-count table unless contrary evidence appears; the moving structure is an observer/run fixture.

### Named six-face majority preset

- Source: `BOOK:19588`.
- Establishes: the cited 3D majority CA uses `UnitStep[a+AxesTotal[a,3]-4]`, so a binary cell is black exactly when old Self plus its six face neighbors contain at least four black values. Its P-completeness statement is a relation/property over the ordinary face-product rule.

### Old-snapshot schedule and finite realization are separate

- Sources: `BOOK:850,10984-10992,11077-11124,16446`.
- Establishes: ordinary CA outputs are computed from old values and committed in parallel; changing to sequential assignment changes the program. The Notes separately describe cyclic arrays, explicit values/backgrounds, `d`-dimensional padding, origin/offset conventions, finite causal widths, and `All`/`Automatic` cropping. Those are seed/work/trace realization data, not T23 topology or UPDATE variants.

### Typed controls remain outside strict T23

- Sources: `BOOK:13542-13549,13642-13648,14234-14237,14336,15074-15075,15485,16446,17002,19072`.
- Establishes: the nearby 2D symmetry-count table belongs to T21/T22; other cubic/crystal/irregular lattices belong to T24; continuous and stochastic cellular automata retain their own value/successor semantics; sequential update is a different schedule; Voronoi and hardware material are relations/controls. None silently widens T23.

## Candidate Case Descriptors to Test

```text
FaceProduct[d=3,k]:
    cases = SelfValue x FaceValueSum[0..6*(k-1)]
    index = SelfValue + k*FaceValueSum
    case_count = k*(6*(k-1)+1)

FullCubeProduct[d=3,k]:
    cases = SelfValue x FullNeighborValueSum[0..26*(k-1)]
    index = SelfValue + k*FullNeighborValueSum
    case_count = k*(26*(k-1)+1)

GeneralFaceContext[k]:
    cases = Alphabet^7 in declared Self-plus-six order

GeneralCubeContext[k]:
    cases = Alphabet^27 in declared `{-1,0,1}^3` order

IgnoreSelfRestriction:
    invariant = outputs constant across Self for equal surrounding aggregate

ShellCountOnly[d=3,k]:
    cases = NeighborValueSum[0..shell_size*(k-1)]
    relation = lossless factor of the corresponding product table
               exactly on the IgnoreSelf-valid image
```

For binary strict profiles, the compact face/full product spaces contain `2^14` and `2^54` rules; the count-only main examples lie in the `2^7` and `2^27` ignore-Self restrictions. General positional tables have respectively 128 and `2^27` input contexts and therefore `2^128` and `2^(2^27)` possible binary rules. These counts are candidate derivations to verify against the complete source and an independent oracle, not new semantic classes.

## Construction Model

- **DOMAIN:** discrete `t+3D`.
- **CONFIGURATION/support:** a total finite-alphabet field on fixed cubic `Z^3` support. Periodic quotients, finite boxes with explicit exteriors, and causal work volumes are realizations. An exact `(uniform background value, finite deviations)` carrier and a coordinate frame are lossless representations; the background may evolve. Crops, projections, and raster/mesh outputs are views. None changes the native topology.
- **ALPHABET:** binary in the direct examples; the Notes use a finite `k`-color alphabet with an explicit numeric valuation for sum-indexed compact tables.
- **Control:** none. The current label field is the complete Markov configuration.
- **FRONTIER:** every old site fires once for an ordinary event. Finite execution lowers that semantic set to the declared work realization without changing program identity.
- **NEIGHBORHOOD:** either the six distinct face offsets or all twenty-six distinct surrounding offsets, with Self composed explicitly only when the rule schema reads it. General positional rules preserve declared offset order; aggregate rules preserve read-occurrence multiplicity even when a quotient makes offsets alias one physical cell.
- **RULE:** one closed same-site finite-alphabet result. Direct predicates are shell-count tables; the Notes additionally define center-plus-shell-value-sum product tables and arbitrary complete positional maps. `IgnoreSelf`, named predicates, and Carter Bays triples are validated restrictions/presets, not evaluator branches.
- **UPDATE:** validate exactly one result for every selected source and atomically assign all results from the same old snapshot. Support and topology remain fixed.
- **Successor/outcome:** one deterministic successor for every valid event. An unchanged configuration or stopped growth is not an inferred halt.
- **Seeds/realizations:** the strict source-backed T23 fixtures are a single cell, a line of three, the Notes' `3x1x1` exact-three control, and its `3x3x1` projection variant. Representing the source's line of three as `3x1x1` additionally requires a declared axis and is a lossless coordinate realization, not source wording. Finite arrays, boundaries, and horizons remain independent configuration/run data. The facing-page row of eleven is T22 control evidence, while random fields are generic/control realization evidence; neither is promoted to a strict T23 fixture.
- **Observers/relations:** octahedral or nested shape, complexity class, moving structures, top projections, depth shading, slices, volumes, and display-axis transforms do not feed native evolution.

## Independent Semantic Oracle

`35-T23-semantic-oracle.py` separates a literal `Native3D` evaluator in raw Book array coordinates from the generic branch-free path of opaque snapshot-scoped site selection, declared access, closed typed RULE evaluation, same-site assignments, and atomic parallel commit. Neither transition evaluator calls the other. The oracle closes 5,139 native/generic events:

```text
face compact quotient bases  4,096
face product fiber witnesses     14
face shell-case witnesses         7
full compact quotient bases     896
full product fiber witnesses     54
full shell-case witnesses        27
directional profiles              34
named structural profiles          9
ternary valuation profiles          2
total                           5,139
```

The compact representation proof expands and factors all `2^14 = 16,384` binary face-product tables over all 128 complete seven-position contexts, checking 2,097,152 expanded rows. It independently covers all 128 shell-only face-output tables, duplicates each table across Self, factors exactly on the equal-Self-fiber image, and rejects a one-Self-row disagreement. For the full cube, enumerating `2^54` tables or `2^27` contexts would be a false coverage claim; instead the oracle proves all 54 product fibers. For each count `n`, each of the two `(Self,n)` fibers has multiplicity `binomial(26,n)`; their combined multiplicity is `2*binomial(26,n)`, and the 27 combined values sum to `2^27`. It covers 56 product zero/full/unit bases, proves 29 shell-only zero/full/unit bases, and rejects both a compact-fiber and an IgnoreSelf violation. Separately, nonaliasing `3x3x3` central fixtures fire every one of the 14 face-product, 7 face-shell, 54 full-product, and 27 full-shell indices through matching unit tables, with native/generic agreement and output one. The alias-heavy small-torus quotient witnesses therefore certify read-occurrence multiplicity but do not carry row coverage.

The general positional domains remain explicit: the seven-position face profile has 128 input rows and `2^128` binary rules; the twenty-seven-position profile has `2^27 = 134,217,728` input rows and `2^(2^27)` rules. Exact mixed-radix address/decode inverses are certified with 56 digit/complement witnesses, and all 27 ordered positional projections prove that no input slot is collapsed. The oracle does not misdescribe compact bases as exhaustive proof of arbitrary full positional tables. The 134,217,728-row binary table is finite (about 16 MiB when ideally bit-packed) but deliberately not materialized in this audit; the space of all such tables is the genuinely intractable quantity.

Raw lexicographically sorted Book triples are authoritative. The explicit audit representation

```text
(Book layer, Book row, Book column)
    -> (runtime x = column, y = -row, z = layer)
```

is labeled `book-layer-row-column_to_runtime-x-east-y-north-z-layer_v1`, has a certified inverse, and is checked on 54 forward/inverse position cases, all 128 binary Self-plus-six contexts, 128 face table bases, 56 full positional digit bases, and an asymmetric naive-re-sort counterexample. It is a chosen right-handed representation extending the T21 row/column adapter, not a semantic inference from `Cuboid[-Reverse[position]]`; that source expression is separately proved to be display-only.

All nine structural presets commute on nonvacuous triggering fixtures: face any, face exact-one, Self-plus-face majority, full exact-one/exact-two/exact-three, and the three Carter Bays triples. The direct predicates retain their source-backed fixtures; majority and Carter Bays use separately identified synthetic center triggers because the source does not serialize their exact 3D seeds. Every successor contains both labels, and the four synthetic center triggers output one at the center. Their derived canonical serializations under `index = SelfValue + 2*ShellCount` are `16380, 12, 16256, 12, 48, 192, 47104, 3584, 11264`; these integers are explicitly marked derived and are not presented as source-given rule numbers. Structural predicates/triples remain primary.

Further adversaries prove all formula cases across selected `(d,k)` pairs, all 64 binary six-shell reads, all 128 binary Self-plus-six contexts, and all 2,187 ternary Self-plus-six contexts. They also prove ternary `d=3` case sizes `13/39` for shell/product, ternary full sizes `53/159`, old-snapshot versus in-place behavior, fixed versus periodic boundaries, native `Z^3` versus finite work and view, exact sparse-background evolution including the nonquiescent `0 -> 1 -> 0` background witness, stale/foreign handles, malformed reads/writes, exact selected-site/write coverage, and T21/T22/T23 use of the identical step function. On the `2x2x2` torus, six face slots resolve with multiplicities `[2,2,2]` and the full shell with `[2,2,2,4,4,4,8]`; deduplicating resolved coordinates therefore changes the program.

The current Dyadaxes summary has two explicit collisions. Empty versus one face neighbor shares `(self=0, face_count>3=false, other_count>=10=false)` but splits the exact-one face and full predicates; a second full-shell pair also splits while the gated summary agrees. Thus current raw rank-three geometry is reusable but the majority/threshold RULE is not a general T23 representation.

## Smallest Goal 2 Composition

```text
Domain        = DiscreteSpace(dimension=3)
Configuration = FixedLattice(OrthogonalGrid(Z^3), Alphabet)
Frontier      = AllSites
Neighborhood  = FaceShell3D | MooreShell3D
              + OptionalExplicit(SelfAt)
Rule          = OrderedContextTable
              | ProductCaseTable(SelfValue, ShellValueSum)
              | ShellAggregateTable(ShellValueSum)
              + ValidatedRestriction(ignore-self/named-predicate)
Update        = SnapshotParallelSameSite
Seed          = IndependentValidatedConfiguration
```

These names describe structural roles for the handoff; they do not mandate construction-specific public classes.

### Candidate implementation delta

1. Reuse the synthesis-selected T01/T21 fixed-lattice configuration, `AllSites`, complete old-snapshot reads, same-site finite-alphabet write, snapshot-parallel UPDATE, deterministic `StepResult`, and support/realization separation. Parameterize the declared regular support by three axes; do not add `ThreeDimensionalState`.
2. Express the six-face and twenty-six-shell profiles through ordinary explicit offsets or the generic L1/L-infinity radius constructors. A `3d` or cubic preset may return those components but may not select rollout.
3. Reuse T02's complete ordered-context map and T03's valuation/aggregate/case-table machinery. Add only any missing closed product/shell case descriptors, factor maps, and validators that are generic across dimension and alphabet.
4. Keep structural tables and schema tags primary. General seven-position and twenty-seven-position tables require respectively 128 and `2^27` context results. Their exact carrier may be an arbitrary-precision integer, immutable packed vector, or finite-domain default-plus-overrides object with validated bounds and outputs; every form must provide exact lookup/serialization round trips. “Random access” never means an unrestricted evaluator callback, and program identity may not be narrowed to `numpy.int64`.
5. Preserve raw sorted Book tuples with an explicit frame. Any signed axis permutation into canonical ENU must carry offsets, contexts, and the complete positional table and prove a commuting inverse. The source's `Cuboid[-Reverse[position]]` remains a separate view transform.
6. Expose the direct any/exact-one/exact-two/exact-three predicates, the Self-plus-six majority threshold, and the three Carter Bays triples as ordinary closed presets over the relevant shell/case schema. The source lists `(4,5,5)`; `B5/S45` is its explicitly derived canonical birth/survival spelling. The other two remain equally structural birth/survival values. None owns an engine.
7. Reuse T08 configuration constructors/laws for points, lines, blocks, and random fields. Bind periodic/fixed boundaries, finite work shapes, horizons, projections, palettes, and moving-structure observations to run/realization/view records, never to the T23 program.
8. Preserve the existing rank-three tensor kernel only as a candidate finite lowering behind the branch-free runner. Remove family-name dispatch during the shared Goal 2 migration; do not route T23 through `dyadaxes_3d`.

### Required conformance areas

1. Reproduce the four direct shell predicates, their exact source seeds, and source-determined checkpoints where available; separately validate the exact-three projection variants and the three class-4 triples.
2. Prove one-step commutation between a literal Book-array evaluator and the generic SimpleProgram path for six-face, twenty-six-shell, positional, product-count, and named-preset rules.
3. Cover all 64 binary six-shell reads and all 128 binary Self-plus-six contexts directly. For the `2^27` full-cube contexts, prove every positional projection and every compact aggregate fiber combinatorially, exercise zero/full/unit bases, and reject a one-row fiber violation without pretending to enumerate the astronomical space of all such rules.
4. Prove exact expansion/factorization for qualifying compact tables and `IgnoreSelf` restrictions; reject interchange among shell-only, `Self x sum`, and positional schemas even when a small numeric spelling coincides.
5. Certify the Book-array-frame-to-ENU signed permutation and its inverse on all seven and twenty-seven positions, carry the complete table, and exhibit an asymmetric case where naive re-sorting changes the result.
6. On small periodic quotients, retain six or twenty-six read occurrences even when several resolve to one cell; reject coordinate deduplication.
7. Distinguish native `Z^3`, finite fixed-exterior work, periodic quotients, sparse background lowerings, and view/crop identities. An exact `uniform background + finite deviations` carrier may evolve its explicit background value. Only a lowering that fixes or elides background evolution requires a quiescent-background proof.
8. Use an order-sensitive fixture to separate old-snapshot parallel UPDATE from in-place scanning and validate exact selected-source/write provenance before commit.
9. Reconstruct the Carter Bays triples and `Self+FaceCount>=4` majority rule as complete product-case predicates. Keep the moving structure and P-completeness statement as trace/property evidence; do not infer untranscribed cells from the raster.
10. Give a concrete Dyadaxes collision—empty shell versus one face neighbor suffices—whose gated summary agrees but whose exact-one T23 result differs.
11. Execute T21, T22, and T23 through the identical generic step function with only dimensional, topology, access, and RULE data changed.
12. Reject missing/duplicate offsets, implicit/duplicate Self, wrong rank/order/frame, incomplete tables, invalid valuation/output, stale reads, wrong targets, duplicate source coverage, unsupported realization maps, and family dispatch.

## Principles Audit and D129

The source and the architecture inference are deliberately separated. The Book directly gives a cubic arrangement; six-face and all-26 surrounding predicates; the `AxesTotal`/`FullTotal` formulas; lexicographically sorted positional offsets and code convention; `Cuboid[-Reverse[position]]` display; three Carter Bays tuples; and the Self-plus-face majority expression. The `Z^3` structural spelling, `6/12/8` face/edge/corner decomposition, 14/54 compact case counts, huge positional rule counts, signed Book-to-ENU adapter, canonical integer serializations, and `B/S` names are labeled mathematical/architecture derivations. None is presented as a quotation.

No evidence supplies a counterexample to the shared SimpleProgram event. T23 natively instantiates its axes at one source event per generic event; this conclusion does not rely on a lower-dimensional CA simulation.

### D129 — T23 parameterizes DOMAIN dimension, cubic CONFIGURATION topology, access, and RULE schemas; projections are views

- **Basis:** the repaired nineteen-query source union closes 151 lines at `104 pre-Index / 47 actual-Index`; 72 retained matches plus 66 governed continuations give 138 evidence lines (`76 native / 16 relation / 46 control`), 32 exclusions, zero unresolved candidates, and retained split closure `123 exact + 15 mapped variants`. The one-hop radius-four asset universe closes 42 plates as `10 governed + 32 adjacency-only`, 84 references, 42 hashes, and `42 hash-bound / 9 transcribed / 0 pixel-replayed`. The semantic oracle closes 5,139 native/generic events, all 16,384 face-product signatures, every face row, all full compact fibers and unit bases, explicit nonaliasing firing of all 14/7 face and 54/27 full product/shell indices, exact positional-address proofs, the frame/table permutation, nonvacuous named predicates, alias multiplicity, and Dyadaxes loss witnesses.
- **DOMAIN/configuration:** use discrete `t+3D` and a total finite-alphabet field on fixed cubic `Z^3` topology. This is the three-axis parameterization of D127/D128's regular fixed-lattice configuration, not a 3D state family. Finite boxes, periodic quotients, fixed exteriors, and causal work volumes are explicit realizations. Exact evolving background-plus-deviations carriers and coordinate frames are lossless representations. Crops, projections, and rasters are views. Only a carrier that fixes or elides background evolution requires a quiescence proof. Other 3D lattices and arbitrary dimension remain T24 evidence boundaries.
- **FRONTIER/read:** reuse `AllSites`. Direct shell-only predicates declare exactly six face or twenty-six surrounding offset reads and no Self read. Product and complete seven-/twenty-seven-position schemas declare exactly one Self plus the shell. Raw Book triples and order are primary; the explicit signed ENU representation carries the complete context/table and has a certified inverse. Coincident quotient cells retain distinct access occurrences.
- **RULE/representation:** shell-only binary case widths are 7 and 27, with rule counts `2^7` and `2^27`. Center-plus-sum product widths are 14 and 54, with rule counts `2^14` and `2^54`. Complete positional tables have 128 and `2^27` input rows, with rule counts `2^128` and `2^(2^27)`. Shell-to-product duplication is lossless exactly on the IgnoreSelf-valid image; compact-to-positional expansion/factorization requires complete constant fibers. Named source predicates/triples and majority are ordinary table values. Derived codes and `B/S` spellings retain provenance and never replace structural identity.
- **RULE result/UPDATE/outcome:** each old firing source returns one complete same-site finite-alphabet write. D004/D117/D127/D128 old-snapshot parallel UPDATE validates complete source coverage, read provenance, targets, values, and one deterministic fixed-support successor. Fixed points or stopped growth do not imply native halt. T08/D121 owns event-zero configurations and laws; shapes, complexity, P-completeness, moving structures, hardware, and renderings remain properties/relations/observers.
- **Current-runtime boundary:** rank-three loci/gather, boundaries, `time_slice`, six-face and twenty-six-shell geometry, tensor episodes, and visualization are mechanically reusable. Current family dispatch, `numpy.int64` rule IDs, schema-poor lookup data, and the 256-rule Dyadaxes experiment are shared implementation gaps or a lossy preset, not evidence for a T23 executor. Current scalar alphabets cover strict T23; product/tagged alphabets remain a broader Goal 2 gap established elsewhere.
- **Consequence:** Goal 2 reuses T01/T02/T03/T08/T21/T22 and the branch-free runner. It adds or exposes only dimension/topology parameters, shell/product/positional case descriptors, exact factor and coordinate/table maps, immutable complete data carriers with bounded lookup/serialization, named presets, realization validation, and conformance fixtures. It adds no unrestricted callback, `ThreeDimensionalState`, control payload, new UPDATE algebra, executor, rollout branch, hidden Self/boundary, Dyadaxes substitution, Life/majority engine, or raster-defined rule. No completed stage reopens.

The decision uses audit categories 1–3 only:

| Axis | Classification | Smallest reusable base | Required invariant/mapping |
|---|---|---|---|
| DOMAIN | 2 | discrete dimensional space | exactly three active spatial dimensions |
| CONFIGURATION | 2 | regular fixed-lattice finite-alphabet field | cubic `Z^3` topology; realization/view separate |
| ALPHABET | 1 | finite scalar alphabet | explicit valuation only for numeric-sum schemas |
| FRONTIER | 1 | `AllSites` | one firing occurrence per old site |
| NEIGHBORHOOD | 2 | ordered offset access with optional explicit Self | 6 or 26 unique shell offsets; zero Self for shell-only, one for product/positional |
| RULE | 2/3 | schema-tagged closed local map | complete fibers, exact codecs, no eager/full-space fiction |
| RULE result / UPDATE / successor | 1 | same-site write + snapshot-parallel commit | exact source/write coverage; one deterministic successor |
| seed / realization / view | 1/2/3 | T08 configuration plus explicit lowering/observer records | no run/view datum in program identity |

## Re-Integration Audit

1. **Prior assumption invalidated?** No active semantic decision fails. D127/D128's dimension-generic fixed-lattice interpretation is confirmed; the repaired localized-structure Index route changes source closure only.
2. **Smallest reuse?** D127 regular fixed support, D128 shell access, T02 complete maps, T03 aggregate factors, T08 configurations, and the shared same-site UPDATE suffice.
3. **Exception, flag, hidden state, duplicate path, or callback?** None. Presets are closed data; large full-cube tables use exact schema/address representations rather than callbacks or family switches.
4. **Complete Markov state?** Yes. The labeled cubic field determines the successor. Boundary/work realization is explicit; projection and renderer state are not hidden transition inputs.
5. **Support/topology/value/control separation?** Yes. `t+3D` is DOMAIN, cubic `Z^3` is CONFIGURATION topology, cell labels are ALPHABET, and T23 has no control factor.
6. **Defining versus incidental algorithm?** All-site old-snapshot parallel commit and declared reads are defining. convolution, tensor layout, sparse work, coordinate adapter, crop, projection, and voxel rendering are implementations/representations/observers.
7. **Encoding fidelity?** Yes. Shell-only and Self-product schemas remain distinct; raw ordered offsets are primary; every coordinate/table/code map is total on its declared image and checked with an inverse or rejection witness.
8. **Stages to reopen?** None. T21/T22 remain strict 2D presets; T24 owns higher dimensions and other lattices; stochastic update semantics remain with their owning stage.
9. **Goal 2 change?** Schedule T23 after shared fixed-lattice/access/table primitives. Add rank-three conformance, exact large-schema representation, and view separation; do not schedule a 3D executor.
10. **Cohesion improved?** Yes. The same runner now covers one-, two-, and three-dimensional fixed lattices, cardinal/Moore/face/full shells, shell-only/product/positional tables, and named Life-like presets without catalog dispatch.

## Audit Questions

1. Which source lines define the strict 3D configuration, firing frontier, access positions, rule cases, same-site result, and update schedule?
2. Are six-face, full-twenty-six, positional, outer-totalistic, totalistic, growth, symmetry, and named profiles restrictions or representations over one fixed-lattice event?
3. What is the exact Book coordinate/order convention, and how must any runtime axis permutation transform the complete table?
4. Which finite boxes, boundaries, backgrounds, seeds, projections, slices, and rasters are realizations or observers rather than native state?
5. Does any stochastic 3D profile require visible transition-time probability/RNG state, or is it only a relation/control for this stage?
6. Where does current `dyadaxes_3d` preserve raw geometry, and give an explicit pair of neighborhoods whose gated summary loses a T23 distinction.
7. Can T21, T22, and T23 execute through one typed runner with dimension/access/rule descriptors and no catalog branch?
8. Does any primary evidence change RULE result, UPDATE composition/schedule, successor cardinality, outcome, or configuration topology beyond fixed cubic support?

## Detailed Implementation Plan

1. Freeze a zero-remainder source query union across the monolith, Notes, actual Index, split corpus, Atlas, catalog, and taxonomy; partition native, relation, control, exclusion, and governed continuation evidence.
2. Derive the complete source-bound asset candidate set and bind every governed or adjacency-only file to source lines and physical hashes without inventing pixel or stochastic replay.
3. Build an independent native/generic semantic oracle for every source-backed strict access/RULE profile, coordinate/table permutation, finite realization boundary, alias multiplicity, and old-snapshot event.
4. Audit `simple_programs.md`, all current `src/ca` modules/tests, D004-D008, D111-D128, and the broader SimpleProgram protocol from first principles.
5. Add D129 only if the evidence establishes a new parameterization/representation boundary; no construction-named class or executor is presumed.
6. Obtain independent hostile review; run root/relocated/optimized/import/compile/Markdown/diff/scope/status/test gates; integrate the plan, evidence index, design ledger, architecture audit, and Goal 2 handoff.

## No-Cheating Checks

- No `three_dimensional`, `3d`, cubic, or T23 rollout branch or executor.
- No finite tensor, periodic boundary, crop, projection, slice, palette, or raster presented as native `Z^3` semantics.
- No hidden Self read, flattened memory-order codec, coordinate re-sort without complete table permutation, or coincident-read deduplication.
- No Dyadaxes majority/threshold summary substituted for raw face/edge/corner values or source-backed aggregates.
- No seed, horizon, shape, volume, class, growth stop, or application fed into program identity.
- No arbitrary CA encoding, callback, family dispatch, or opaque state packing accepted as native one-step reuse.

## Completion Requirements

- [x] Exhaustive source/split/Notes/Index/alias audit closes with zero unresolved candidates.
- [x] The declared one-hop source-bound asset candidate universe closes with honest claim boundaries.
- [x] Exact 3D access, RULE schemas/restrictions, coordinate mappings, and named fixtures are independently proven.
- [x] Smallest reusable base is classified with a concrete-counterexample gate for any claimed new algebra.
- [x] Current API/runtime/principles audit and Goal 2 handoff are implementation-ready.
- [x] Independent hostile review and all oracle/test/Markdown/diff/scope/coverage gates pass.
- [x] `0-plan.md`, `evidence-index.md`, `design-ledger.md`, and `architecture-audit.md` agree.

## Stage Results

COMPLETE. The nineteen-query source oracle closes 151 lines at `104 pre-Index / 47 actual-Index`; 72 retained matches plus 66 governed continuations give 138 evidence lines (`76 native / 16 relation / 46 control`), 32 exclusions, zero unresolved candidates, and retained split closure `123 exact + 15 mapped variants`. The declared one-hop radius-four asset universe closes 42 plates as `10 governed + 32 adjacency-only`, 84 references, 42 hashes, and `42 hash-bound / 9 transcribed / 0 pixel-replayed`.

The independent semantic oracle closes 5,139 native/generic events at `4,096 face quotient + 14 face-product fibers + 7 face-shell cases + 896 full quotient + 54 full-product fibers + 27 full-shell cases + 34 directional + 9 named + 2 ternary`. It fires every compact index on nonaliasing fixtures, proves the complete face tables and full fiber arithmetic, makes all named and ternary executions discriminating, preserves raw Book order through a certified frame/table map, retains quotient read multiplicity, distinguishes representations/realizations/views, and exhibits two Dyadaxes information-loss collisions.

D129 classifies T23 entirely through audit categories 1–3: discrete `t+3D`, cubic `Z^3` CONFIGURATION, six-/twenty-six-shell access, and schema-tagged shell/product/positional RULE data parameterize the shared fixed-lattice SimpleProgram event. No state/control class, UPDATE algebra, executor, family branch, hidden Self/boundary, callback, or raster-defined rule is added. The independent hostile review in `35-T23-hostile-review.md` is clean after its source-boundary, sparse-representation, access-fidelity, fiber-coverage, seed-provenance, formula, and test-strength repairs. Root and relocated oracles, optimized-mode fail-closed, silent-import, compile, Markdown, diff, scope, coverage, and all 102 repository tests pass. No prior stage reopens. Next: T24.
