# 17-T41-FUNCTIONS

Status: **IN PROGRESS**

## Current Facts

- Exact catalog row: T41, CSV line 42, `Function-Combination Systems`; taxonomy vocabulary is `ref/notes/CA-Types.md:1132-1158`. T42 begins at taxonomy line 1160.
- Canonical main heading `Mathematical Functions` begins at `BOOK:1834`; T43 `Iterated Maps and the Chaos Phenomenon` begins cleanly at `BOOK:1868`, making `1834-1866` the strict main range.
- The main section distinguishes standard mathematical functions with simple curves, finite combinations such as sums of sine functions with more complicated curves, and the Riemann-Siegel Z function as a named complex example.
- The object under study appears to be a mathematical function/curve, not a time-evolving numeric state. Plotted samples, zero crossings, spacing sequences, waveforms, and axis-crossing patterns must be audited as queries/observers rather than assumed transitions.
- Page 162's continued-fraction-driven generalized substitution system belongs materially to T42 even though it occurs inside the T41 main section. T41 must retain the source function and crossing query without absorbing T42's generated rule schedule/update semantics.
- An exact closed expression tree may reuse T20's structural tree carrier responsibility, but mathematical evaluation/denotation is not symbolic rewriting. Host callables or a formula string would erase inspectability and equality.
- A continuous mathematical domain, a finite plot interval, an approximate sampling mesh, and exact/root-isolation queries are different scopes. Raster resolution cannot become native function semantics.
- Current `FormulaRule`/selector callbacks and NumPy/plotting paths may evaluate or render examples, but no existing evidenced closed function-expression spec, real/complex domain, root-event record, numerical context, or function-denotation category is yet established.
- Exact figures, parameters, Notes/Index routes, aliases, history, sampling/crossing conventions, equality/serialization, and current API/runtime fit are under parallel audit.

## Updated Assumptions

- Separate a closed mathematical function specification from evaluation queries and rendered/sampled observations.
- Do not add a transition source/read/result/update merely because a curve is traversed left-to-right or sampled at successive x values.
- Expression data will use an explicitly supported finite operator/function vocabulary with declared arities/domains; no `Callable`, `eval`, symbolic host object, pickle, or opaque string.
- Exact algebraic identities, numerical agreement, and structural expression identity will remain different relations unless evidence justifies a canonical quotient.
- Real versus complex argument/codomain, branch conventions, poles/undefined values, precision, interval endpoints, sampling mesh, root multiplicity, and event ordering will be explicit where relevant.
- T42 substitution, T43 iteration, T44 continuous lattice evolution, T45 PDEs, Fourier/spectral observers, sound rendering, and zeta/prime relations remain typed boundaries until evidence proves reuse.

## Big Picture Objective

Reconstruct finite combinations of mathematical functions as inspectable continuous-function specifications with separately typed evaluation, sampling, plotting, and zero-event queries. Pin down closed expression syntax, domains/codomains, exact versus numerical semantics, singular/branch behavior, figure presets, T42/T43/T44/T45 boundaries, current API/runtime pressure, and the smallest honest Goal 2 category without inventing false dynamics.

## Catalog Identity

- Stable ID: T41.
- Exact name: Function-Combination Systems.
- CSV provenance: `ref/notes/CA-Types.csv:42`; taxonomy provenance `ref/notes/CA-Types.md:1132-1158`.
- Canonical strict main: `BOOK:1834-1866`; clean split lines under audit.
- Native Notes begin at `BOOK:13146`; T43 Notes begin at `13215`; the internal T41/T42 Notes boundary is under content-based audit.
- Entry kind, strict closed-function profile, named function set, query profiles, aliases, variants, and relations are under audit.

## Search Log

1. Read the catalog/taxonomy seed, strict main range through the T43 boundary, and the native Notes heading through the T43 Notes boundary.
2. Opened parallel exact figure/profile, exhaustive source/Notes/Index/search, and architecture/API/runtime audits.
3. Direct names, function-combination vocabulary, sine/cosine sums, zero crossings, waveforms, Lissajous/Fourier/Weierstrass/zeta routes, history, programs, actual Index, and false positives are in progress.

## Book Excerpts

Canonical excerpt groups are under audit.

## Construction Model

The audit must decide at least these categories independently:

1. a closed mathematical expression denoting a real/complex function;
2. a finite evaluation or sampled-curve query with declared numerical context;
3. an exact or certified zero/crossing/extremum query and its ordered event record;
4. downstream raster, sound, spacing, spectrum, or symbolic/analytic observer;
5. a T42 generated substitution program driven by a parameter expansion, which is not T41 execution.

State/update applicability, expression syntax, domains, values, singularities, equality, serialization, exact/approx evaluation, query outcomes, and observer boundaries remain under audit.

## Exact Book Presets and Oracles

Page-160 standard curves, page-161 sine combinations, page-162 crossing/substitution relation, page-163 Riemann-Siegel Z curve, Notes formulas, plot horizons, parameters, hashes, and independent anchors are under reconstruction.

## Variants, Relations, and Boundaries

T20 symbolic expressions, T27 point-map ASTs, T31 constraints, T39 numerical observers, T42 driven substitution, T43 iterated maps, T44 continuous CA, T45 PDEs, Fourier series, sound, special functions, and analytic solvers are under audit as native syntax, queries, observers, alternate definitions, or distinct constructions.

## Current API Fit

In progress.

## Current Runtime Fit

In progress.

## Principles Audit

The source currently supports a specification/query category rather than another transition law. No update member will be proposed unless an actual old-state-to-new-state construction appears. Formula callbacks, fixed sampling grids, raster identity, and host symbolic evaluation are rejected as semantic shortcuts pending the full audit.

## Detailed Implementation Plan

1. Complete strict main/raster, Notes, actual Index, split, history, program, alias, variant, cross-reference, and false-positive searches.
2. Decode every strict figure formula, parameter, interval, plot/query convention, and exact or independent numerical anchor.
3. Derive closed expression, domain/codomain, evaluation/query, event, outcome, equality, serialization, and observer categories from evidence.
4. Separate T42 substitution traces, T43 iteration, continuous fields/PDEs, analytic identities, sounds, plots, and numerical algorithms.
5. Audit current API/runtime/tests and completed primitives; write an implementation-ready Goal 2 handoff without callbacks or a function rollout branch.
6. Integrate global artifacts and run source/oracle, fence, coverage, diff, and repository verification before closure.

## Goal 2 Implementation Stage

Pending the evidence audit.

## No-Cheating Checks

The final stage must reject unrestricted function/root callbacks, `eval`/formula strings, host symbolic objects as portable program data, sampled arrays or rasters as the function, mesh-dependent equality, undeclared precision/branches, finite plot intervals as native domains, traversal/sampling as fake time evolution, T42/T43 execution substituted for T41 denotation, and family-specific rollout dispatch.

## Completion Requirements

- [ ] Every strict figure, main/Notes/actual-Index/split passage, history item, alias, named function/variant, program, and relation candidate is dispositioned.
- [ ] Closed expression, domain/codomain, exact/numerical evaluation, singularity/branch, query/event/outcome, equality, serialization, and observation semantics are explicit.
- [ ] Page-160/161/162/163 profiles have independent exact or declared-precision oracles and source repairs.
- [ ] T20/T42/T43/T44/T45, analytic solver, sampling, plotting, sound, and spectrum boundaries are explicit.
- [ ] Current API/runtime fit, Goal 2 files/dependencies/tests, global ledgers, diff checks, and repository tests are integrated.

## Stage Results

In progress.

## Integration Results

In progress.
