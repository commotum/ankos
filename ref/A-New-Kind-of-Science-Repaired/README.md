# A New Kind of Science — repair worktree

This directory is generated by `goal-5/build.py` from the immutable legacy OCR monolith. It is a provisional baseline projection, not yet a completely source-verified or OCR-corrected edition.
