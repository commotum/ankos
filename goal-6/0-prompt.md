# Goal 6 Continuation Prompt

```text
Work through goal-6/0-plan.md using goal-6/0-loop.md.

The objective is to remaster the frozen Goal 2 architecture and implementation
plan around SimpleProgram(seed, alphabet, frontier, neighborhood, rule), the
finalized core/catalog structure, and all 60 executable semantic families
audited by Goal 5.

Read goal-6/0-plan.md and goal-6/0-loop.md in full, inspect the actual repository
state, update current facts, and execute only the first incomplete stage. Use
goal-5/taxonomy-census.md, goal-5/11-FAMILIES.md, and
goal-5/api-pressure.md as semantic/API authority; follow
goal-5/integration-handoff.md for the remaster boundary and
goal-5/10-RECONCILE.md for T01–T45. Use Goal 2 only for the explicitly
preserved design strengths. Do not reopen Book discovery or import Goal 4's
audit machinery.

Keep exactly five stored program fields. Frontier is the complete writable
envelope, Neighborhood is the readable region, and Rule owns applicability,
scheduling, conflict, stochastic, result, and atomic update semantics. Preserve
closed serializable descriptors, exact values, visible control and entropy,
typed cardinality/outcomes, witnesses before deduplication, provenance,
lossless codecs, and a family-blind universal application law.

Use the locked core files and catalog names:
program.py, loci.py, alphabets.py, seeds.py, frontiers.py, neighborhoods.py,
rules.py, serialization.py, py.typed, and catalog/{entries,automata,substitua,
machina,media,criteria,dynamica}.py. Whole-program aliases belong in the
catalog; component presets belong in their plural modules. The 60 families are
coverage and constructors, not runtime subclasses or dispatch cases. Defer
generation/datasets/streams/RNG/viz internals except for necessary public
boundaries.

For each iteration: sync actual state, update the plan, create or refresh the
stage file from the loop template, do only that stage, verify its real
requirements, record results, fold durable facts back into the plan, and leave
the next step resumable.

Goal 6 changes planning, reference, and documentation artifacts only. Do not
make behavioral changes under src/ca, modify frozen Goal 2, or begin Goal 7.
Completion means the five contracts and generic application semantics are
explicit, all 60 families and every T01–T45 action have an exact catalog plan,
the pressure/conformance review passes, current docs agree, and one
mechanics-first file-level Goal 7 handoff is ready. Carry any unresolved issue
forward as explicit next work rather than hiding it or weakening the goal.
```
