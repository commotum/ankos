# 5-CATALOG

## Current Facts

- G7-04 starts from clean commit
  `cf88affc29e225f64aaeca200c6f6548ecc42512`.
- The complete active baseline reports `413 passed, 13 skipped`. The focused
  catalog/coverage/role baseline reports `71 passed, 13 skipped`.
- All thirteen skips are owned by this stage: five CT11 tests, five catalog
  unit tests, two constructor/metadata family joins, and one F010/F042 role
  test.
- G7-00 through G7-03 are complete. Exactly five stored program fields, one
  family-blind `apply`, reusable mechanics for all sixty audited families,
  and the closed catalog-free codec are authoritative prerequisites.
- The eight catalog files physically exist as inert shells. Sixty canonical
  exact-slug signatures raise `NotImplementedError`; presets and the sole
  compatibility adapter remain private pending-name inventories; three true
  alias signatures are also inert.
- `catalog.entries` defines frozen callable-free record shapes but no SPF,
  role, legacy, target, or name values.
- Root `ca` exposes eleven names and does not yet expose `catalog`.
- `goal-6/catalog-migration.md` freezes exactly 60 families, 2 close roles,
  45 legacy rows, 49 callable legacy relations, six homes, every canonical
  signature, every public spelling, and every migration disposition.

## Updated Assumptions

- Category constructors can share private composition helpers only when those
  helpers operate on explicit closed component inputs and never select
  behavior from SPF/F/T identity, category, constructor spelling, or metadata.
- The existing reusable mechanics are sufficient. If an honest constructor
  needs a missing semantic descriptor or application behavior, G7-02 and
  G7-03 must be reopened before catalog implementation continues.
- Metadata may provide explicit immutable tuples and descriptive lookup by ID
  or spelling, but lookup returns metadata only and cannot construct, execute,
  encode, or mutate a program.
- Presets must have explicit closed keyword-only signatures and validations.
  A family-level alias in source research is still a `P` callable when it
  binds semantic construction data, as T32 and T44 do.
- The one `K` adapter, `machina.extended_mobile_automaton`, must be total and
  lossless over one explicitly advertised legacy argument domain, issue a
  deprecation warning, and remain absent from flat `ca.catalog.__all__`.

## Big Picture Objective

Turn the inert catalog shells into the exact ordinary-program construction and
callable-free navigation surface frozen by Goal 6: sixty canonical families,
all authorized presets/aliases/the one adapter, the complete SPF/F/T/name
metadata join, explicit collision-free exports, and no catalog authority over
application or serialization.

## Detailed Implementation Plan

1. Freeze executable test manifests directly from the Goal 6 authority.
   - Transcribe all sixty family rows, two close roles, T01–T45, and every
     callable name relation into implementation-independent expected data.
   - Assert exact IDs, counts, homes, coverage, candidate joins, targets,
     callable kinds, sources, bindings, owners, signatures, and flat flags.
2. Populate callable-free metadata.
   - Add explicit immutable `FAMILIES`, `ROLES`, `LEGACY_ENTRIES`, and
     `NAMES` tuples in `catalog/entries.py`.
   - Validate closed record invariants without importing category modules or
     storing callables, components, programs, Rule tags, or dispatch handlers.
   - If convenience lookup is exposed, return metadata only from immutable
     explicit values; never expose `construct(id, ...)`.
3. Implement the six category modules.
   - Replace every canonical stub once in its locked primary home.
   - Give all canonical, preset, alias, and compatibility callables explicit
     keyword-only signatures with closed validation and no `*args`,
     `**kwargs`, ignored keyword, silent coercion, callback, or opaque recipe.
   - Compose only existing Seed, Alphabet, writable/readable, Rule, and
     `SimpleProgram` structures; never import `entries`.
   - Make aliases exact delegates, presets exact closed restrictions, and the
     compatibility adapter a total lossless translation.
4. Assemble the public catalog.
   - Make `catalog.__init__` the sole explicit callable/metadata join.
   - Export all sixty canonical names and all authorized `C/P/A` spellings
     flat; keep the sole `K` category-qualified; expose no `M` callable.
   - Add only the `catalog` namespace to root `ca`.
5. Activate the owned conformance obligations.
   - Replace the CT11 and catalog skeletons with positive and hostile
     parameterized tests using literal expected manifests.
   - Join each canonical constructor to one representative closed argument
     set, exact metadata, and its already-authoritative mechanics fixture.
   - Activate F010/F042 role metadata and the two constructor/metadata
     family-coverage tests.
6. Close the stage.
   - Run focused catalog, CT11, family, observer, public/import, serialization,
     and owner tests; then run the complete active suite.
   - Run static no-dispatch/import/callable-metadata/pending-stub/signature and
     codec-isolation checks, lock validation, compilation, and
     `git diff --check`.
   - Conduct one hostile review, record exact evidence here, reconcile
     `0-plan.md`, and stop before creating or implementing G7-05.

Expected production owners are the eight files under `src/ca/catalog/` and
`src/ca/__init__.py`. Expected test owners are `tests/test_catalog.py`,
`tests/conformance/test_catalog_expansion.py`,
`tests/conformance/test_family_coverage.py`,
`tests/conformance/test_observer_boundary.py`, plus a test-only literal
manifest/helper when that keeps the expected ledger independent.

## No-Cheating Checks

- Category modules do not import `catalog.entries`; `catalog.__init__` is the
  only metadata/callable join.
- Core/program/serialization modules never import catalog, and catalog
  imports do not change `apply`, rollout, codec, or program identity.
- Metadata contains no callable, component value, `SimpleProgram`, Rule tag,
  executor key, registry hook, mutable mapping, or construction handler.
- No production code selects construction or behavior by SPF/F/T ID, audit
  family, category, constructor name, Book source, carrier, or locus kind.
- There is no `construct(id)`, umbrella `kind=`, dynamic registration,
  synthesized function, module `__getattr__`, wildcard discovery, or second
  executor.
- Every public callable has an explicit keyword-only signature with no
  variadic arguments, ignored arguments, opaque `Any`, unrestricted callback,
  hidden solver, ambient RNG, or silent float/coercion fallback.
- All sixty canonical functions are defined exactly once in their owner
  module. Delegators contain no independent component or application logic.
- Every program is exactly `type(value) is SimpleProgram`, has the exact five
  stored fields, validates, codec-round-trips without catalog identity, and
  applies through the one generic operation.
- T08 alone has zero targets, T40 alone has two named targets and no umbrella
  callable, every other T row has one target, and F010/F042 remain
  callable-free roles.
- All 48 `C/P/A` legacy spellings are explicit flat exports; the sole
  deprecated `K` remains category-qualified; `M` relations are non-callable;
  T32/T44 remain presets.
- Goal 2, Goal 5, Goal 6, completed Goal 7 mechanics/codecs, datasets, RNG,
  visualization, and release documentation remain unchanged unless a concrete
  earlier-stage defect is first recorded and reopened.

## Completion Requirements

- SPF IDs are exactly SPF001–SPF060; executable audit IDs, slugs, constructor
  names, and primary homes are unique and exact.
- Home counts are exactly `11 / 15 / 8 / 14 / 9 / 3`; coverage is exactly
  `19 covered / 41 addition`.
- F010/F042 are exactly the two close roles, F039 remains unused, and no role
  acquires a family constructor.
- Legacy IDs are exactly T01–T45 with disposition counts
  `15 / 21 / 2 / 3 / 2 / 1 / 1`; candidate/source joins and target
  cardinalities match the authority row by row.
- Callable legacy relations are exactly `C=5 / P=39 / A=4 / K=1`, with exact
  spelling, owner, target, binding/translation, sources, and flat-export
  behavior.
- Exactly sixty canonical constructors plus every authorized delegator has a
  closed explicit signature and returns an ordinary compatible five-field
  program composed only from codec-covered semantic variants.
- Every alias/preset/adapter passes exact expansion equality over its declared
  domain; T32/T44 are proven semantic bindings rather than zero-delta aliases.
- All sixty canonical programs join exact SPF/F/home/source/pressure metadata
  and run their already-authoritative named generic mechanics obligation.
- `ca.catalog` is exposed as the twelfth root name; no catalog constructor is
  flattened to root `ca`.
- CT11, CT14's role boundary, catalog unit tests, and the constructor/metadata
  halves of family coverage pass with all thirteen G7-04 skips removed.
- Static and hostile review find no pending stub, callable metadata, dynamic
  registry, family dispatch, guessed compatibility path, catalog-backed
  codec, second executor, missing family, or unauthorized export.
- The full active suite is green with no G7-04-owned skip. G7-05 remains
  unstarted and Goal 7 is not called release-ready.

## Stage Results

- Pending implementation and verification.
