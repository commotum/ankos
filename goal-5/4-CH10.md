# 4-CH10

## Current Facts

- Chapter 10 contains 14 headings and its Notes contain 12 headings.
- The pair totals 2,112 lines and approximately 59,000 words.
- No existing raw lead cites either Chapter 10 document.
- Discovery remains blind to the catalog, API, runtime, and prior-goal prose.

## Updated Assumptions

- Most of the chapter concerns observers, measurements, compression criteria,
  perception, analysis, and properties rather than state-transition systems.
- Some compression, cryptographic, neural, symbolic, or mathematical procedures
  may nevertheless specify executable constructions.
- A procedure that consumes an input and returns one output can be a
  construction even when it is not iterated, but a metric or explanatory
  analogy is not.

## Big Picture Objective

Discover executable and relation-defining procedures in Chapter 10 while
keeping observers, measurements, properties, and interpretive models out of the
construction count unless their mechanics independently warrant inclusion.

## Detailed Implementation Plan

- Read the chapter and Notes fully and sequentially.
- Record all 26 headings as covered.
- Register one compact lead per coherent executable procedure or genuine
  construction/observer ambiguity.
- Resolve metrics, properties, examples, and application labels cheaply.
- Inspect a figure only when its visual content supplies an algorithm,
  transformation, state representation, or ambiguity absent from prose.
- Update the raw register, coverage map, stage result, and plan.

## No-Cheating Checks

- Do not consult the catalog, API, runtime, or prior-goal material.
- Do not promote randomness, complexity, entropy, compression ratio, visual
  salience, or statistical significance merely because it is measurable.
- Do not create separate leads for named codecs, ciphers, formulas, or
  perception examples that share already captured mechanics.
- Do not build a paragraph ledger or duplicate the source read.

## Completion Requirements

- All 14 chapter and 12 Notes headings are covered sequentially.
- Every retained lead has canonical source anchors and a Book-derived trigger.
- Executable procedures are distinguished from observers, measurements,
  properties, and examples.
- Any serious observer/construction boundary has a concise reason.
- Every inspected figure has a taxonomy-bearing reason.
- `coverage.md` records 14/14 and 12/12.
- Changes remain confined to Goal 5, `git diff --check` passes, and artifact
  growth remains compact.

## Stage Results

### Coverage

- One reader covered the 1,014-line chapter and 1,098-line Notes file fully and
  sequentially; no duplicate reader was used.
- Covered all 14 chapter headings at lines 3, 5, 23, 63, 119, 159, 269, 307,
  387, 429, 535, 637, 805, and 963.
- Covered all 12 Notes headings at lines 1, 3, 25, 29, 177, 290, 392, 482,
  514, 664, 955, and 1087.

### Compression and transformation procedures

- `L1509` (`WEAK`): self-delimiting unary, length-prefixed, ternary-derived,
  and Fibonacci integer codecs are supporting representations, not autonomous
  systems (`CH:163-179`; `N:34-82`).
- `L1510–L1515` (`SERIOUS`): run-length, Huffman block, arithmetic,
  pointer/Lempel–Ziv, recursive-subdivision, and orthogonal-basis
  transform/truncation procedures each define a concrete input-to-output
  transformation with an inverse or explicitly lossy variant (`CH:163-305`;
  `N:83-288`).

### Perception and analysis procedures

- `L1516` (`SERIOUS`): local-template or weighted-threshold feature extraction
  slides a kernel over an image and emits response cells; its
  construction-versus-observer status remains open (`CH:323-357`; `N:298-334`).
- `L1517–L1518` (`SERIOUS`): ordered dithering uses a recursively constructed
  threshold matrix, while error diffusion scans sequentially and distributes
  quantization error to future neighbors (`N:339-360`).
- `L1519` (`WEAK`): waveform/note sonification is an output mapping
  (`CH:397-405`; `N:419-423`).
- `L1520` (`SERIOUS`): linear-predictive sound coding fits local predictors and
  codes residuals against a finite codebook (`N:424`).

### Statistical and probabilistic procedures

- `L1521` (`SERIOUS`): a local-factor relation assigns sequence weights by
  multiplying factors over overlapping blocks (`N:493-494`).
- `L1522` (`SERIOUS`): fixed-topology IID/block/Markov fitting estimates
  outgoing probabilities from observed frequencies, then samples paths
  (`CH:441-459`; `N:495-501`).
- `L1523` (`SERIOUS`): probabilistic CA fitting estimates output distributions
  for each spacetime neighborhood and samples new histories (`CH:461-477`).
- `L1524` (`SERIOUS`): an arbitrary program can act as a statistical test by
  comparing observed-data output with outputs from randomized inputs; this is a
  deliberate constructor-versus-analyzer ambiguity (`CH:515-533`).

### Cryptographic and inverse procedures

- `L1525` (`SERIOUS`): XOR stream encryption combines plaintext with a
  key-derived stream; repeating, LFSR/additive-CA, and sparsely sampled rule-30
  streams are generator variants (`CH:539-565`, `599-605`).
- `L1526–L1527` (`SERIOUS`): inverse CA/LFSR analysis uses sideways evolution,
  modular linear solving, or depth-first light-cone assignment with inconsistent
  branches pruned (`CH:575-633`; `N:531-544`, `608-624`).
- `L1528` (`WEAK`): RSA modular exponentiation is exact but provisionally
  belongs to a general arithmetic-relation family (`N:642-654`).
- `L1529` (`RESOLVED`): iterated squaring modulo `m` plus a parity observer is
  an arithmetic iterated map with an output projection (`N:656-660`).

### Symbolic and mathematical transformations

- `L1530` (`SERIOUS`): convert a uniform cuboidal substitution into a coordinate
  automaton that reads position digits and returns a cell without generating
  intermediate patterns (`CH:647-673`; `N:672-681`).
- `L1531` (`RESOLVED`): power cellular automata are the earlier integer
  multiplication systems expressed as local digit evolution (`CH:729-743`;
  `N:745-749`).
- `L1532` (`SERIOUS`): associative evolution uses repeated squaring to compose a
  rule according to the binary digits of a requested step count
  (`CH:743-757`; `N:790-810`).
- `L1533` (`SERIOUS`): finite light-cone enumeration followed by Boolean
  minimization produces an executable formula or analysis artifact
  (`CH:759-801`; `N:816-903`).
- `L1534` (`SERIOUS`): reversible Boolean evaluation embeds an irreversible
  function in a permutation circuit with ancillary outputs (`N:904`).

### Memory, learning, language, and interaction

- `L1535` (`SERIOUS`): exact hashing folds a symbol sequence into a bounded
  integer and resolves collisions by chaining or probing (`CH:829-839`;
  `N:976-980`).
- `L1536` (`SERIOUS`): a layered sparse-template hash aims to preserve
  similarity, creating a memory-construction versus perceptual-observer
  ambiguity (`CH:851-879`).
- `L1537` (`SERIOUS`): nearest-memory retrieval uses Voronoi cells, spatial
  trees, or descent to stored minima (`N:988-996`).
- `L1538` (`WEAK`): Hamming encoding/decoding is supporting error-correction
  infrastructure (`N:998-1017`).
- `L1539–L1540` (`SERIOUS`): matrix associative memory projects and thresholds
  a query; neural rules update feedforward or recurrent states and learn
  weights, with asynchronous schedules deferred to the Chapter 9 family
  comparison (`N:1019-1023`).
- `L1541–L1542` (`SERIOUS`): symbolic pattern rewriting transforms matching
  expression subtrees; context-free derivation and reverse parsing retain one or
  multiple derivations (`CH:909-915`; `N:1038-1065`).
- `L1543` (`SERIOUS`): two programs repeatedly generate moves from both output
  histories and receive payoffs from a matrix (`N:1081-1085`).
- `L1544` (`UNREVIEWED`, later dependency): exhaustive enumeration of candidate
  descriptions is executable under finite cutoffs but unrestricted recognition
  depends on the Chapter 12 halting boundary (`CH:975-989`; `N:5-15`).

Chapter 10 adds 36 leads: 29 serious, two resolved, four weak, and one explicit
later dependency. These procedures are expected to collapse substantially into
uniterated-function, relation, observer, codec, and existing evolution families
during Stage 8.

### Selective image decision

- No original-resolution figure was required.
- The candidate figures contain example codec record layouts, detector
  templates, model topologies, sideways-rule diagrams, search trees, or hash
  presets. The surrounding canonical prose specifies the family-defining
  transformation; the visual particulars are parameters or worked examples.
- In particular, the rule-30 inverse and CA backtracking procedures are
  completely distinguished by `CH:600-630` and `N:531-544,608-624`, so their
  figures are not needed for taxonomy.

### Deliberate exclusions and dependencies

- Randomness/complexity definitions, entropy and compression ratios,
  spectra/correlations/frequencies, formula or BDD sizes, security/period
  claims, biological interpretations, historical material, presets, and output
  behaviors remain properties, measurements, examples, or interpretations.
- Stage 8 must compare the CA, substitution, finite-automaton, constraint,
  term-rewrite, arithmetic, recurrent-update, and observer-like leads with their
  earlier mechanics.
- Chapters 11–12 remain necessary for exhaustive recognition, parsing,
  cryptanalytic hardness, universality, and undecidability boundaries.

### Completion

- `raw-leads.csv` now contains 1,544 leads.
- `coverage.md` records 14/14 chapter and 12/12 Notes headings.
- Every retained lead has canonical anchors and a Book-derived trigger.
- No catalog, API, runtime, predecessor, or prior-goal material was consulted.
- Next: Stage 5, `CH11`.
