# 6-CH12

## Current Facts

- Chapter 12 contains 13 headings and its Notes contain 13 headings.
- The pair totals 3,529 lines and approximately 107,500 words.
- No existing raw lead cites either Chapter 12 document.
- This is the final sequential chapter-discovery stage before the Index check.

## Updated Assumptions

- Most of Chapter 12 concerns principles, computational limitations,
  mathematics, intelligence, technology, and historical interpretation rather
  than new executable mechanics.
- Halting, proof, axiom, theorem-enumeration, and optimization procedures may
  nevertheless define functions or relations relevant to partiality and
  constraint families.
- Named undecidable problems and individual formal systems are properties,
  instances, or presets unless the Book supplies mechanically distinct
  construction rules.

## Big Picture Objective

Discover any remaining executable or relation-defining mechanics in Chapter 12
while preventing principles, impossibility results, complexity claims,
mathematical objects, and historical examples from inflating the taxonomy.

## Detailed Implementation Plan

- Read the chapter and Notes fully and sequentially.
- Record all 26 headings as covered.
- Register one compact lead per coherent mechanism or genuine
  construction-versus-relation ambiguity.
- Carry partiality, halting, infinite search, and limit-defined questions
  explicitly into consolidation.
- Inspect only figures that contain family-defining mechanics absent from
  prose.
- Update the raw register, coverage map, stage result, and plan.

## No-Cheating Checks

- Do not consult the catalog, API, runtime, or prior-goal material.
- Do not promote the Principle of Computational Equivalence, computational
  irreducibility, undecidability, intractability, free will, intelligence, or
  universality as constructions.
- Do not create separate leads for every theorem, axiom system, undecidable
  problem, machine preset, or technological application.
- Do not build a proof ledger or duplicate the source read.

## Completion Requirements

- All 13 chapter and 13 Notes headings are covered sequentially.
- Every retained lead has canonical source anchors and a Book-derived trigger.
- Executable mechanics and relations are distinguished from principles,
  properties, instances, proofs, and interpretations.
- Partial and nonterminating procedures have explicit later dispositions.
- Every inspected figure has a taxonomy-bearing reason.
- `coverage.md` records 13/13 for both documents.
- Changes remain confined to Goal 5, `git diff --check` passes, and artifact
  growth remains compact.

## Stage Results

### Coverage

- One reader covered the 1,686-line chapter and 1,843-line Notes file fully and
  sequentially; no duplicate reader was used.
- Covered all 13 chapter headings at lines 3, 5, 23, 65, 155, 269, 293, 461,
  511, 723, 1291, 1587, and 1647.
- Covered all 13 Notes headings at lines 1, 3, 7, 16, 33, 94, 100, 174, 196,
  581, 1506, 1763, and 1823.

### Registered mechanics and relations

- `L1552` (`SERIOUS`): a Turing-machine partial function encodes input on tape,
  evolves until an explicit halt/readout predicate, and has no value for
  nonhalting inputs. Halt state, head boundary, tape pattern, and fixed-point
  policies are observably different (`CH:571-607`; `N:207-209`, `235-237`,
  `365-369`).
- `L1553` (`SERIOUS`): the Post correspondence relation uses one finite index
  sequence to concatenate the upper and lower words from ordered pairs and
  accepts exactly when the results match (`CH:541-555`; `N:241-291`).
- `L1554` (`SERIOUS`): multiway tag evolution applies every matching prefix
  rule on every branch. The source exposes a multiplicity/order versus
  deduplicated-state-set distinction (`N:269-304`).
- `L1555` (`SERIOUS`): a sequence-equation relation assigns finite words to
  variables, flattens concatenations, and tests equality (`N:314-316`).
- `L1556` (`SERIOUS` after final hostile review): a fixed compare-exchange
  network applies an ordered list of index-pair sorts, optionally parallelizing
  disjoint pairs. It is an executable finite-gate-circuit variant
  (`N:331-347`, `461-476`).
- `L1557` (`SERIOUS`): a nondeterministic Turing transition maps each
  state/symbol pair to several write/state/move outcomes and advances every
  branch, with explicit deduplication and existential acceptance
  (`CH:647-677`; `N:495-503`).
- `L1558` (`UNREVIEWED`, later dependency): CNF satisfaction is a Boolean
  relation, and a bounded-trace compiler translates nondeterministic-machine
  histories into it; Stage 8 must decide whether this is a relation plus
  compiler rather than another construction (`CH:681-691`; `N:521-543`).
- `L1559` (`UNREVIEWED`, later dependency): a quantum circuit applies a finite
  sequence of one- and two-qubit unitary gates and samples squared amplitudes;
  finite amplitude, tolerance, normalization, measurement, and randomness
  policies remain unspecified (`N:560-574`).
- `L1560` (`SERIOUS`): bidirectional symbolic axiom rewriting applies both
  directions of equations to expression subtrees or whole formulas. RHS-only
  variables require an explicit fresh-expression enumeration policy
  (`CH:749-775`; `N:605-611`, `751-797`).
- `L1561` (`SERIOUS`): a Diophantine relation accepts nonnegative integer
  assignments satisfying polynomial or exponential equations; auxiliary
  variables compile finite computations into existential constraints
  (`CH:885-905`; `N:901-966`).
- `L1562` (`SERIOUS`): a finite operator table evaluates expression trees under
  every variable assignment, while an axiom system accepts tables satisfying
  every equation for every assignment (`CH:1073-1095`; `N:1189-1203`,
  `1245-1257`).
- `L1563` (`WEAK`): time-compressed continuous computation maps unbounded source
  time to a finite limiting endpoint whose precision, convergence, and readout
  are unspecified (`CH:227-239`; `N:49-75`).
- `L1564` (`SERIOUS`, added by final hostile review): the
  Friedberg–Muchnik-style construction fairly dovetails many suspended
  register-machine computations against shared mutable finite approximations,
  then enumerates diagonalizing cells and injures affected lower-priority work
  (`N:80-92`).

Chapter 12 adds 13 leads: ten serious, one weak, and two explicit later
dependencies.

### Selective image decision

- Final hostile review inspected
  `BACK-MATTER/NOTES/_page_1157_Figure_7.jpeg` because its five wiring diagrams
  materially distinguish repetitive, nested, and irregular fixed comparator
  schedules and show which disjoint comparisons can share a layer. Together
  with the executable `PairSort` prose at Notes lines 331–347, it confirms
  L1556 as a finite-gate-circuit preset rather than weak exposition.
- It also inspected `BACK-MATTER/NOTES/_page_1146_Figure_2.jpeg` because the
  successive states show persistent shared bottom approximations, many
  suspended upper computations, and temporary agreements later destroyed.
  Together with Notes lines 80–92, this establishes L1564 as an executable
  priority/dovetail construction rather than an oracle or computability
  property.
- PCP witness searches and finite operator models remain complete in prose and
  formulas and required no additional figure inspection.

### Resolved non-leads and dependencies

- Word problems are reachability in bidirectional multiway rewriting.
- Multiway/operator translations and operator/symbolic translations are carrier
  encodings once the source mechanisms exist.
- Lemmas, proof networks, named logical calculi, groups, semigroups, universal
  equations, and individual axiom systems are rules, presets, or instances.
- Stage 8 must consolidate partial functions and unbounded search (`L1544`,
  `L1551–L1552`), multiway/tag variants (`L1554`), Boolean/CNF relations
  (`L1533–L1534`, `L1558`), and equation/constraint relations (`L1496`,
  `L1506`, `L1521`, `L1553`, `L1555`, `L1561–L1562`).

### Deliberate exclusions

- The Principle of Computational Equivalence, computational irreducibility,
  free will, intelligence, purpose, technology, and historical interpretation
  are not constructions.
- Undecidability, intractability, complexity-class membership, proof length,
  lower bounds, diagonal/Gödel/Rice arguments, oracle hierarchies, and
  universality are properties or results.
- Individual machines, rule numbers, named problems, theorem lists, universal
  equations, axiom sets, and technological examples are instances or presets.

### Completion

- `raw-leads.csv` contains 1,564 leads after the final hostile-review addition.
- `coverage.md` records 13/13 headings for both documents.
- Every retained lead has canonical anchors and a Book-derived trigger.
- No catalog, API, runtime, predecessor, or prior-goal material was consulted.
- Next: Stage 7, `INDEX-CHECK`.
