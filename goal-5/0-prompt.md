# Goal 5 Continuation Prompt

```text
Work through /home/jake/Developer/ankos/goal-5/0-plan.md using /home/jake/Developer/ankos/goal-5/0-loop.md.

The objective is to replace the overbuilt Goal 4 effort with a lean, finishable repair of the local A New Kind of Science Markdown corpus. Start by inspecting and removing superseded Goal 4 plans, ledgers, locks, schemas, pipeline code, tests, caches, and unverified staging output. Preserve unrelated work and keep ref/A-New-Kind-of-Science byte-for-byte immutable.

Then build ref/A-New-Kind-of-Science-Repaired from the immutable legacy inputs as a practical repository edition: exactly 29 correctly owned author-text documents; repaired chapter/Notes/Index/Colophon boundaries; all 1,444 legacy image references accounted for and resolving without unnecessary asset duplication; improved Markdown structure; useful contents, anchors, and Notes navigation; explicit evidence for every author-text change; and a concise disclosure of unresolved OCR, layout, figure, formula, code, caption, and Index limitations.

Do not recreate Goal 4's generalized schemas, proof locks, authority/reviewer framework, per-block witness mapping, hostile race defenses, or exhaustive certification process. Prefer one straightforward builder, one focused validator, a compact repair file, and tests tied to real acceptance criteria. Do not guess author text or claim publication-grade fidelity. Preserve uncertain text and document the limitation.

At each stage, sync the actual files, tests, and Git state; update 0-plan.md; create or refresh the stage file from the loop template; implement only that stage; run its focused and affected full checks; record evidence; fold durable findings back into the plan; and leave the work resumable if stopping.

Completion means the original practical objective is genuinely achieved: Goal 4 excess is cleaned up safely, the repaired sibling builds deterministically from untouched legacy inputs, structural and navigation requirements pass, applied text repairs are evidence-backed, known limitations are honest, and unrelated consumers remain unchanged. Carry open issues forward as explicit next work rather than silently narrowing or falsely certifying the result.
```
