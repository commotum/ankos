# Goal 5 Continuation Prompt

```text
Work through goal-5/0-plan.md using goal-5/0-loop.md.

The objective is to finish a rigorous but lean whole-book ANKoS construction
taxonomy, reconcile it with T01–T45, determine the real semantic families,
pressure-test SimpleProgram(seed, alphabet, frontier, neighborhood, rule), and
produce a compact integration handoff.

Start by reading only goal-5/0-plan.md and goal-5/0-loop.md in full. Treat this
as the sole active completion contract for the unfinished taxonomy audit. Keep
all predecessor artifacts preserved but inert: do not open or rely on their
plans, prose, tools, histories, validators, search archives, or generated
outputs. The only exception is Stage 1's one-time streaming projection of raw
lead IDs into a compact Goal 5 register and the data-only source-ID mapping
needed to derive canonical anchors and short surface triggers directly from the
Book; never load predecessor data wholesale into context or import its
conclusions.

Use the repaired canonical Book as discovery authority. Close Chapter 8, then
read Chapters 9–12 and their Notes sequentially. Record heading-level coverage
and only construction-bearing or genuinely ambiguous evidence. Treat raw leads
cheaply, inspect only taxonomy-relevant figures, use the Index as an omission
checklist, consolidate by mechanics, and run one whole-book saturation pass.
Keep discovery blind to T01–T45, Goal 1/2 conclusions, the proposed API, and the
runtime until the plan's reconciliation stage.

Do not build audit infrastructure: no per-paragraph negative ledger,
transaction history, replay system, accepted-output programs, duplicate
bundles, generalized validators, identical search reruns, giant raw dumps, or
redundant multi-agent rereads. Use compact Markdown/CSV, bounded projections,
source anchors, lightweight structural checks, and one final independent
hostile review.

For each iteration: sync actual state, update current facts in the plan, select
the first incomplete stage, create or refresh its stage file from the loop
template, perform only that stage, verify its real requirements, record results,
fold durable findings back into the plan, and leave the next step resumable.

Do not modify the Book, current catalog, API, runtime, tests, or prior goals
without separate user authorization. Completion means the original taxonomy and
API-pressure objective is genuinely answered, every serious candidate and
T01–T45 entry has a source-grounded disposition, the hostile review is
resolved, and open issues are carried forward as explicit next work rather than
hidden or declared out of scope.
```
