# 9-CH05-DIMENSIONS

Status: **IN PROGRESS**.

## Current Facts

- Stages 1–8 are complete. Stage 9 begins from the verified Stage 8 terminal
  state without pre-reading any Chapter 5 source unit or inspecting any
  Chapter 5 image.
- The live blind-discovery state contains 14,311 source units, 2,186 reviewed
  units, 656 active blind candidates, 202 routes (75 resolved and 127
  pending), 1,607 physical images of which 361 are screened, and 12 closed
  LOCAL rounds.
- Review history is complete through `V000022`; the active global discovery
  epoch is 2.
- Stage 9 owns exactly two canonical documents:
  - `CHAPTERS/05-Two-Dimensions-and-Beyond.md`;
  - `BACK-MATTER/NOTES/05-Two-Dimensions-and-Beyond-Notes.md`.
- The main chapter contains 276 source units, `U000947..U001222`, and owns 80
  referenced physical images, `A000843..A000922`.
- The Notes contain 263 source units, `U006076..U006338`, and own 70 physical
  images, `A000519..A000588`: 37 referenced images and 33 unreferenced
  physical images assigned by unique directory/page range.
- The paired assignment therefore contains exactly 539 source units and 150
  physical images. All 539 reading rows and all 150 asset rows are `PENDING`.
- The Stage 8 gate passes in ordinary and optimized Python:

  ```text
  validated blind audit harness: units=14311 reviewed=2186 candidates=656
  routes=202 assets=1607 screened=361 rounds=12
  ```

- The six mutable-ledger starting hashes are:
  - `reading-ledger.csv`:
    `ffeada81cc7fd287920ba34a15a4c38ff11bc6763fc256f3f5c0942c85fa4b5b`;
  - `candidate-ledger.jsonl`:
    `11c1da78335cc690161e1f65cf1a4446cfd0915409c7d7164fd7b649440d32d9`;
  - `cross-reference-ledger.csv`:
    `7c4b254601904a04530ae1859cd528c54e834c57fe716af043e7697bc979137f`;
  - `asset-ledger.csv`:
    `ebe56581896a81e638a2e39f5b6f5d8567abe055464e85613ae6a5310a39ddf5`;
  - `search-rounds.json`:
    `c7e90fa50726bbc91eefe9630f26a60127b124950401cf6fba0ac2cbd4557fb5`;
  - `review-history.jsonl`:
    `3ceccad97e2a008e17f4ce3f9cc85fc51a6ee6675308f313dd02cf413c0f059d`.

## Updated Assumptions

- The Chapter 5 main text and Notes form one paired discovery stage, but their
  sequential reviews must remain isolated in separate sealed bundles until
  both outputs are finalized.
- Topology, dimension, structural replacement, graph identity, branching,
  constraints, schedules, and witness semantics are review prompts, not
  advance findings or candidate identities.
- A higher-dimensional display is not automatically a higher-dimensional
  native carrier. A projection, slice, stacking convention, history display,
  or embedding must remain distinct from the construction it represents.
- A structural relation, constraint, solver, multiway history, and rendering
  remain distinct unless the assigned source establishes their identity.
- Under-specified boundary conditions, neighborhoods, orientations, update
  schedules, gluing rules, branch semantics, and equivalences must remain
  explicit source-limited unknowns.
- Visual similarity, a familiar rule icon, or a nearby caption cannot supply
  hidden mechanics.

## Big Picture Objective

Blindly audit the complete Chapter 5 main-text and Notes assignment for every
source-grounded construction, generator, relation, constraint, preset,
restriction, representation, observer, application, numerical procedure, and
genuine source boundary, with particular care for topology, dimension,
structural replacement, network identity, branching, schedules, and witness
semantics.

## Allowed Inputs And Scope

The stage-opening setup may use only:

- `goal-4/0-plan.md`, `goal-4/0-loop.md`, and verified Stage 8 terminal facts;
- Goal 4 guardrails, corpus manifest, current blind ledgers, blind schemas,
  and coordinator tooling needed to create and verify sealed review bundles.

Once sequential review begins, each isolated reviewer may additionally use
only its own sealed assignment:

- the main reviewer: `U000947..U001222` and the 80 images owned by
  `CHAPTERS/05-Two-Dimensions-and-Beyond.md`;
- the Notes reviewer: `U006076..U006338` and the 70 images owned by
  `BACK-MATTER/NOTES/05-Two-Dimensions-and-Beyond-Notes.md`;
- sanitized blind-discovery instructions, blind schemas, and write-free
  verification material included in the sealed bundle.

After both sequential reviews are complete, LOCAL search may read exactly the
same two canonical paths and no other Book path.

Forbidden throughout Stage 9:

- T01–T45 mappings, the current catalog, and `CA-Types` conclusions;
- Goal 1 and Goal 2 conclusions;
- `api.md`, `simple_programs.md`, `src/ca`, or any API-fit, executor, runtime,
  or implementation-support judgment;
- any unassigned Book text or image, except that a reference to it may be
  recorded as a pending `CROSS_RANGE` route;
- the other reviewer's semantic output before both sealed reviews are
  finalized and returned to the root coordinator.

## Source Coverage

| Assignment | Source units | Reviewed | Physical images | Screened |
|---|---:|---:|---:|---:|
| Chapter 5 main text | 276 | 0 | 80 referenced | 0 |
| Chapter 5 Notes | 263 | 0 | 37 referenced + 33 unreferenced | 0 |
| **Total** | **539** | **0** | **150** | **0** |

Starting route/search state:

- 75 routes are resolved and 127 are pending globally.
- The exact incoming Stage 9 subset will be resolved only after its target
  units have been reached in sequential review.
- No Stage 9 `WITHIN_STAGE` or outgoing `CROSS_RANGE` route has been created.
- Stage 9 search begins from 12 closed LOCAL rounds; no Stage 9 query or hit
  exists.

## Candidate Changes

- No candidate has been created, changed, split, or merged for Stage 9.
- Worker proposals use independent ordered `W####`, `WE######`,
  `WG######`, and `WR####` identifiers. Only the coordinator may allocate
  global B, E, G, and R identities after both outputs are verified and
  combined in canonical traversal order.
- Candidate capture follows the frozen identity-plus-semantic-anchor rule
  without a target count.
- Every candidate must carry all fingerprint keys, per-field support status,
  exact result kind, missing mechanics, uncertainties, image witnesses, and
  typed routes.
- Main/Notes proposals that look similar remain separate unless the strict
  blind identity-proof contract is satisfied from assigned evidence.

## Search And Evidence Log

No Stage 9 source review, image inspection, route closure, or search has
begun.

The required order is:

1. finish and verify both complete sequential reviews;
2. combine and atomically merge their outputs;
3. resolve incoming and within-stage routes whose targets are now reviewed;
4. freeze candidate-derived and guardrail query families;
5. search exactly the two assigned paths;
6. disposition every query/unit hit in context;
7. rerun the identical search family to a real zero-delta result.

The Stage 9 zero-delta rerun is local closure only, not the Stage 18
whole-corpus saturation fixed point.

## Detailed Implementation Plan

1. Reconfirm the six starting hashes, `V000022`, active epoch 2, corpus map,
   and Stage 8 ordinary/optimized gates before semantic work.
2. Build two sealed epoch-2 bundles from the same pending ledger state and
   verify their disjoint source/asset partitions, hashes, prompts, and
   prohibited-input declarations.
3. Prepare nonsemantic worksheets with no default dispositions, candidates,
   visual roles, evidence strengths, or inferred mechanics.
4. Review each bundle independently in source-unit order. Read every unit in
   full context and create/link candidates only under the frozen capture rule.
5. Screen all 150 owned assets, including all 33 unreferenced Notes images.
   Inspect construction-bearing, text-bearing, ambiguous, or
   caption-incomplete assets at original resolution and independently check
   mechanics-bearing transcriptions.
6. Record source-limited topology, dimensionality, carrier/support identity,
   boundary conditions, neighborhoods, schedules, branching, structural
   invariants, relation/constraint laws, result kinds, and witness semantics.
7. Subject both outputs to hostile semantic, visual, and provenance review.
   Repair authoring sources and regenerate new bundles rather than patching a
   finalized output.
8. Verify both constituents, combine them in canonical traversal order, prove
   constituent-order invariance, and preview the union merge in ordinary and
   optimized Python.
9. Apply exactly one atomic Stage 9 `INITIAL` transaction and revalidate
   six-ledger replay/history-prefix integrity.
10. Resolve every reachable incoming and within-stage route through one
    previewed `ROUTE_RESOLUTION`; leave future targets as typed obligations.
11. Freeze the Stage 9 LOCAL query family, disposition every hit, apply one
    `SEARCH_APPEND`, and apply an identical zero-delta rerun.
12. Run ordinary/optimized Stage 9 gates, full Goal 4 regressions,
    corpus/hash verification, mutation/self-tests, byte compilation, and
    whitespace/scope checks.
13. Record terminal counts, IDs, hashes, defects, routes, search digests, and
    exact Stage 10 handoff here and in `0-plan.md`.

## No-Cheating Checks

- Preserve the all-pending baseline: 539/539 assigned units and 150/150
  assigned assets must be accounted for with no gap, overlap, or semantic
  default.
- Main and Notes reviewers receive disjoint sealed assignments and cannot
  inspect each other's semantic output before both finish.
- No heading, caption, filename, keyword, or pending route substitutes for
  sequential in-context reading.
- Direct image mechanics requires original-resolution inspection, contextual
  anchoring, and independently checked transcription.
- Worker-local IDs follow immutable discovery traversal; global IDs are
  allocated only during coordinator preview/apply.
- No future range may be opened merely to resolve a route.
- LOCAL query-scope union must equal exactly the two Stage 9 paths. Every hit
  must reproduce and have one final disposition.
- Blind outputs must remain free of T mappings, catalog actions, API fit, and
  runtime conclusions.
- Green validators establish integrity only; they do not establish semantic
  understanding.

## Completion Requirements

- All 276 main and 263 Notes units are individually reviewed: **not yet met**.
- All 80 main and 70 Notes images are screened at required depth:
  **not yet met**.
- Both split outputs and their canonical union pass ordinary/optimized
  verification with complete disjoint coverage: **not yet met**.
- Every candidate has complete source-limited provenance, fingerprint,
  result kind, uncertainty, and missing-mechanics records: **not yet met**.
- Exactly one atomic Stage 9 `INITIAL` transaction is applied and replays:
  **not yet met**.
- Every reachable incoming and Stage 9 within-stage route is resolved:
  **not yet met**.
- Every exact-scope LOCAL hit is dispositioned and an identical zero-delta
  rerun is applied: **not yet met**.
- Ordinary/optimized Stage 9, regression, mutation, corpus, history, scope,
  compilation, and whitespace gates pass: **not yet met**.
- This file and `0-plan.md` record the exact Stage 10 handoff:
  **not yet met**.

## Stage Results

Stage 9 is open from the verified Stage 8 terminal state. Only blind-safe
assignment accounting has occurred. No Chapter 5 text, formula, caption, or
image has been semantically reviewed.

The next safe action is to build and verify the two sealed epoch-2 Stage 9
worker bundles, then begin independent sequential review at `U000947` in the
main bundle and `U006076` in the Notes bundle.
