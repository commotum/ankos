# Goal 4 Continuation Prompt

```text
Work through /home/jake/Developer/ankos/goal-4/0-plan.md using /home/jake/Developer/ankos/goal-4/0-loop.md.

The objective is to produce a source-faithful, correctly organized, navigable, and deterministically reproducible Markdown edition of the complete local A New Kind of Science corpus, including prose, Notes, formulas, Wolfram Language code, figures/captions, and the multi-column Index.

Preserve the existing ref/A-New-Kind-of-Science corpus byte-for-byte as immutable legacy evidence because Goal 1/3 oracles and citations depend on its exact paths, hashes, and lines. Build the repaired edition alongside it through reversible, hash-guarded repair overlays. Never use generated output as the next build's input and do not migrate legacy consumers without separate authorization.

Correct author text only from pinned edition-identical authoritative evidence. The monolith and split files are correlated OCR derivatives, and the local JPEGs are extracted illustrations rather than complete page scans. Model guesses, spell-checking, parsing, rendering, execution, mathematical plausibility, and nearby-image placement may generate candidates but are not proof. Preserve literal source errors separately from OCR repair, and leave unsupported ambiguities explicitly unresolved.

At the start of each stage, sync the actual files, hashes, witnesses, ledgers, tests, worktree scope, and prior results; update 0-plan.md; select the first incomplete stage; create or refresh its stage file from the template; implement only that stage; add provenance, inverse, visual-review, no-cheating, and mutation checks; run focused and applicable full verification; record results; and fold discoveries back into the plan.

Do not mark a stage complete on green tooling alone. Every assigned source block/page and candidate must be reviewed and dispositioned, every repair must be reversible and evidence-traceable, every high-risk technical/figure/Index edit must receive independent review, and immutable raw scope must remain clean.

Completion means the original full-repair objective is genuinely achieved: a reproducible repaired release with correct structure, complete raw-to-repaired provenance, all 1,444 assets governed, all navigation resolved, authoritative sequential text review, specialist formula/code/figure/Index closure, hostile review, rollback data, and no unqualified high-risk unresolved defect. Carry any genuine blocker forward as explicit next work rather than silently narrowing the goal.
```
