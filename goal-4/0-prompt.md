# Goal 4 Continuation Prompt

```text
Work through /home/jake/Developer/ankos/goal-4/0-plan.md using /home/jake/Developer/ankos/goal-4/0-loop.md.

The objective is to produce a source-faithful, correctly organized, navigable, and deterministically reproducible Markdown edition of the complete local A New Kind of Science corpus, including prose, Notes, formulas, Wolfram Language code, figures/captions, and the multi-column Index.

Preserve the explicit existing ref/A-New-Kind-of-Science input allowlist byte-for-byte as immutable legacy evidence because Goal 1/3 oracles and citations depend on its exact paths, hashes, lines, and recursive behavior. Build the repaired edition in the new sibling folder ref/A-New-Kind-of-Science-Repaired through reversible, hash-guarded overlays. Never place output inside the legacy root, use generated output as the next build's input, or migrate legacy consumers without separate authorization.

Correct author text only from pinned edition-identical authoritative page-region evidence. The monolith and split files are correlated OCR derivatives, and the local JPEGs are extracted illustrations rather than complete page scans. Model guesses, spell-checking, parsing, rendering, execution, mathematical plausibility, and nearby-image placement may generate candidates but are not proof. Preserve literal source errors in canonical author text, put errata/search/editorial metadata in separate targets, and leave unsupported ambiguities explicit.

At the start of each stage, sync the actual files, hashes, witnesses, ledgers, tests, worktree scope, and prior results; update 0-plan.md; select the lowest-numbered incomplete stage whose declared dependencies and witness regions are ready; create or refresh its stage file from the template; implement only that stage; add provenance, inverse, visual-review, no-cheating, and mutation checks; run focused and applicable full verification; record results; and fold discoveries back into the plan. Dependency-independent stages may proceed around a source-blocked region, but only one stage is in progress and release remains blocked.

Do not mark a stage complete on green tooling alone. Enforce two-way coverage: every monolith block and authoritative page region must reach the 29 canonical documents or an explicit blocker, and every canonical span must map to raw text or a witness-backed anchored insertion. Every candidate must close or enter one governed specialist/reviewer workflow state, every repair must be reversible and evidence-traceable, every high-risk technical/figure/Index edit must receive independent review, and immutable raw scope/consumer behavior must remain clean.

Completion means the original full-repair objective is genuinely achieved: a reproducible repaired sibling release with correct structure; distinct canonical, aggregate, editorial, and search roles; complete bidirectional raw/witness/repaired provenance; all 1,444 legacy assets plus every printed visual component governed; all navigation resolved; authoritative sequential text review; specialist formula/code/figure/Index closure; hostile review; rollback data; unchanged legacy-oracle behavior; and no unresolved source-needed authorial defect of any severity. Carry any genuine blocker forward as explicit next work rather than silently narrowing the goal.
```
