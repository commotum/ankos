# 5-CH01-FOUNDATIONS

Status: **IN PROGRESS**.

## Current Facts

- Stages 1–4 are complete.
- The valid live state contains 157 reviewed source units, 2 screened images,
  2 active blind candidates, 4 pending cross-range routes, and 2 closed Stage
  4 LOCAL rounds.
- Stage 5 owns exactly two canonical documents in manifest order:
  - `CHAPTERS/01-The-Foundations-for-a-New-Kind-of-Science.md`;
  - `BACK-MATTER/NOTES/01-The-Foundations-for-a-New-Kind-of-Science-Notes.md`.
- The paired paths contain 142 deterministic source units:
  `U000070..U000173` and `U004924..U004961`.
- They own 3 physical images, all referenced from the main chapter; the Notes
  path owns no physical image.
- Stage 5 will be one epoch-1 `INITIAL` transaction followed by one or more
  epoch-1 `LOCAL` search transactions and any applicable route transactions.

## Updated Assumptions

- Chapter 1 is conceptual framing, but conceptual prose may still delimit an
  unnamed mechanism or formal object. No unit receives a bulk exclusion.
- A named system or field without both an identity anchor and semantic anchor
  is history/context, not a candidate.
- A displayed example or image is mechanics evidence only to the extent that
  its surrounding source establishes the construction.
- Stage 4 vocabulary and candidates may guide local recall only after
  sequential reading; they do not authorize a disposition in this paired
  range.

## Big Picture Objective

Blindly audit Chapter 1 and its Notes for every construction-bearing system,
formal object, preset, restriction, input/seed class, representation,
application, or source defect while preserving the Book-first boundary.

## Allowed Inputs And Scope

Allowed:

- Goal 4 plan, loop, guardrails, corpus map, harness, and current blind
  ledgers;
- only the two assigned Chapter 1 paths, their 142 source units, and their
  3 owned images;
- deterministic LOCAL searches over exactly those two paths after sequential
  review.

Forbidden:

- current catalog mappings or identifiers;
- prior type conclusions;
- API-fit, executor, or implementation-support conclusions;
- unassigned Book ranges except through explicit pending routes.

All writes remain under `goal-4/`; sealed temporary review material may be
created under `/tmp`.

## Source Coverage

Starting coverage:

| Obligation | Assigned | Completed |
|---|---:|---:|
| Source units | 142 | 0 |
| Physical images | 3 | 0 |
| LOCAL search scopes | 2 paths | 0 |
| Search hits | 0 | 0 |
| Within-stage routes | 0 | 0 |
| Cross-range routes opened | 0 | 0 |

Every unit and image requires an individual disposition, source/evidence
status, concise evidence statement, epoch/stage/reviewer metadata, and exact
candidate/route links.

## Candidate Changes

No Stage 5 candidate has been proposed. Any discovery receives the next
worker-local `W####` identity in frozen traversal and a global `B####`
identity only through coordinator preview/apply.

## Search And Evidence Log

No Stage 5 query has run. LOCAL search begins only after all 142 units are
reviewed and all 3 images are screened.

## Detailed Implementation Plan

1. Build and verify one sealed epoch-1 bundle for both paired paths.
2. Prepare the exact nonsemantic 142-row/3-asset worksheet.
3. Read every source unit in canonical order and record only source-grounded
   dispositions, evidence, candidates, and routes.
4. Screen all 3 images, escalating construction-bearing, text-bearing,
   ambiguous, or caption-incomplete assets to original resolution.
5. Complete all provisional fingerprints and missing-mechanics boundaries.
6. Verify the completed bundle, preview its canonical allocations, and apply
   one atomic `INITIAL` transaction.
7. Run frozen candidate-derived and guardrail LOCAL queries over exactly both
   reviewed paths, disposition every hit, and repeat to a zero-delta round.
8. Resolve all Stage 5 `WITHIN_STAGE` routes and retain legitimate
   `CROSS_RANGE` routes for their owning review stages.
9. Run ordinary/optimized Stage 5 gates, mutation/self-tests, full relevant
   regressions, corpus/hash checks, and diff/scope checks.
10. Record exact results here and update `0-plan.md` to the Stage 6 handoff.

## No-Cheating Checks

- Sequential reading and visual inspection precede search.
- The worksheet has no semantic defaults.
- All immutable row/asset identities remain bundle-bound.
- Worker-local IDs follow frozen discovery traversal and are allocated
  globally only by the coordinator.
- Blind outputs remain free of reconciliation and architecture conclusions.
- LOCAL scope union must equal exactly the two reviewed paths.
- Green tooling does not substitute for source reading.

## Completion Requirements

- All 142 source units are individually reviewed.
- All 3 images are screened at the required depth.
- Every candidate has complete source-limited provenance and fingerprint.
- Every relevant reference is resolved locally or queued with typed scope.
- Every LOCAL hit has a final blind disposition and exact two-path scope.
- No Stage 5 `WITHIN_STAGE` route remains pending.
- `validate_audit.py --require-stage 5` passes in ordinary and optimized
  Python.

## Stage Results

In progress. Exact transaction IDs, candidates, evidence, routes, searches,
decisions, and verification results will be recorded after execution.
