# 7-CH03-PROGRAMS

Status: **COMPLETE**.

## Current Facts

- Stages 1–7 are complete.
- The valid live state contains 1,441 reviewed source units, 216 screened
  images, 320 active blind candidates, 178 routes (37 resolved and 141
  pending cross-range), and 8 closed LOCAL rounds.
- Stage 7 owns exactly two canonical documents in manifest order:
  - `CHAPTERS/03-The-World-of-Simple-Programs/03-The-World-of-Simple-Programs.md`;
  - `BACK-MATTER/NOTES/03-The-World-of-Simple-Programs-Notes.md`.
- The paired paths contain 653 deterministic source units:
  `U000306..U000640` and `U005319..U005636`.
- They own 133 physical images: 87 referenced from the main chapter, 45
  referenced from the Notes, and unreferenced physical asset `A000407`
  assigned to the Notes by its unique directory/page range.
- Stage 7 is recorded by epoch-1 transactions `V000012..V000015`: one
  `INITIAL`, one `ROUTE_RESOLUTION`, and two `SEARCH_APPEND` transactions.

## Updated Assumptions

- Chapter 3 overlaps the historical seed scope of the existing catalog, but
  no existing catalog row, T-ID, Goal 1 conclusion, or familiar semantic name
  may be used during this blind stage.
- A named rule or system may be a native construction, specialization,
  seed/boundary class, observer, representation, property, application,
  emulation, or historical locator. The source must decide the role.
- Rule diagrams and evolution images can supply evidence only after their
  context and original-resolution contents are inspected.
- Similar displayed behavior does not establish semantic-family identity.

## Big Picture Objective

Blindly audit Chapter 3 and its Notes for every construction-bearing system,
formal object, preset, restriction, input/seed class, representation,
application, and source defect without importing the catalog originally
derived from this material.

## Allowed Inputs And Scope

Allowed:

- Goal 4 plan, loop, guardrails, corpus map, harness, and current blind
  ledgers;
- only the two assigned Chapter 3 paths, their 653 source units, and their 133
  owned images;
- deterministic LOCAL searches over exactly those two paths after sequential
  review.

Forbidden:

- current catalog mappings or identifiers;
- Goal 1 type conclusions or Goal 2 implementation names;
- API-fit, executor, or implementation-support conclusions;
- unassigned Book ranges except through explicit pending routes.

All writes remain under `goal-4/`; sealed temporary review and visual-screening
material may be created under `/tmp`.

## Source Coverage

| Obligation | Assigned | Completed |
|---|---:|---:|
| Source units | 653 | 653 |
| Physical images | 133 | 133 |
| LOCAL search scopes | 2 paths | 2 |
| Search hits | 2,496 | 2,496 |
| Incoming routes resolved here | 11 | 11 |
| Within-stage routes | 16 | 16 |
| Cross-range routes opened | 61 | 61 |

The reading dispositions are 169 `CANDIDATE`, 182 `SUPPORTS_CANDIDATE`, 29
`CROSS_REFERENCE`, 89 `REPRESENTATION_OR_OBSERVER`, 6
`APPLICATION_OR_EMULATION`, 15 `HISTORICAL_ONLY`, 161 `NO_CONSTRUCTION`, and
2 `SOURCE_DEFECT_OR_AMBIGUITY`.

All 133 images were screened. Their visual roles are 89 native-evidence, 37
observer, 4 source-defect, 2 relation, and 1 decorative record. Original
resolution was reviewed for 122 images and explicitly not required for 11;
94 limited transcriptions were checked.

## Candidate Changes

The coordinator allocated `B0092..B0320`, 229 deliberately uncollapsed blind
coverage candidates:

- `B0092..B0140` cover elementary and totalistic cellular-automaton families,
  presets, codecs, seeds, restrictions, equivalence relations, surveys, and
  queries.
- `B0141..B0151` cover mobile-automaton families, presets, surveys, and
  compression observers.
- `B0152..B0160` cover Turing-machine families, seeds, presets, and surveys.
- `B0161..B0180` cover parallel, neighbor-dependent, creation/deletion, and
  sequential substitution families, presets, representations, and observers.
- `B0181..B0192` cover tag and cyclic-tag families, restrictions, presets,
  seeds, observers, and queries.
- `B0193..B0202` cover register-machine families, programs, restrictions,
  observers, and derived maps.
- `B0203..B0208` cover symbolic rewrite families, codecs, surveys, queries,
  and observers.
- `B0209..B0320` preserve Notes-derived cellular-automaton, algebraic,
  mobile/Turing, substitution/sequence/transducer, tag, register, and
  symbolic/operator candidates.

Duplicate-looking main-text and Notes candidates remain independent blind
coverage records. No catalog identity, semantic-family equivalence, T-ID
mapping, or API-fit judgment is made here.

## Search And Evidence Log

Search began only after all 653 units were reviewed, all 133 images were
screened, and the governed route-resolution transaction was applied.

- `S007` froze 14 regex query families over exactly the two assigned paths.
  It produced 1,248 fully dispositioned hits across 595 unique units, added
  77 exact ordered source-derived vocabulary terms, and added no candidate,
  evidence group, route, reading, or asset semantic change. The per-family
  hit counts are
  `[134,46,49,83,22,34,37,27,112,254,221,32,43,154]`.
- The main path contributed 606 hits across 297 unique units; the Notes path
  contributed 642 hits across 298 unique units.
- Each round's disposition partition is 710 governed candidate/support hits,
  303 exclusions, 170 controls/relationships, and 65 cross-references.
- `S008` repeated the same 14 families, 1,248-hit result, 595-unit coverage,
  and semantic dispositions, with no new vocabulary or semantic delta.
- The two rounds cover all 229 Stage 7 candidates. Candidate reach includes
  123 direct-`PROSE` exact-unit witnesses and the frozen 266-case F01–F10
  omission challenge.

The frozen query-specification digest is
`408c853ceeb968411e59e95545275bfadc705802dfb164bf929b2e70219dea6e`;
the normalized family/unit result digest is
`5ccc2a695d43a72c7b2c27a7bad1db9ebafc5f730cea19a0e789a8057bd17038`;
and the triage-projection digest is
`6b6d104ab3428ac7cf626f1999b44c6856826f6a4d96ad763100a9b1e228e3df`.
The 229/229 candidate-coverage digest is
`0b7ba7f55d209c3963a03649b721708e1ff4fd49346c5e0a85d38c0ec8e8f437`,
and the omission-challenge digest is
`1e8b273abca746fbdd4b041205f8e0952db133de23bcff7c3e4296d07d0f067c`.

The `S007` proposal and round digests are
`9869fae0847a2e55e0a240dea2b1ff467e7f88cff757010db1f0c794c94c4ebe`
and
`c9bc158bd041632fefa42fa18a6ef29d2b99bf00f07f82bbe5cc6075ee950b4c`.
The `S008` proposal and round digests are
`0fca4fc0dc037eb6beef10ac1042c67a83d819c29cad0f0795af088fd157188f`
and
`1db66ae15ff96b6cad92519d50f5bee7cb7eb454ebae462258cf8621554c6375`.
The final `search-rounds.json` SHA-256 is
`9561cfe25932ca5da92634ce220e61ac1644cc29f1d0ad75a67bcd49fdefe76f`.
All eight LOCAL rounds through Stage 7 contain 4,098 hit records. This
stage-local zero-delta rerun does not claim the Stage 18 whole-corpus fixed
point.

## Cross-Reference Closure

`V000013` resolved 11 incoming routes whose reviewed targets occur in Chapter
3:

`R000007`, `R000015`, `R000018`, `R000019`, `R000021`, `R000029`,
`R000050`, `R000078`, `R000080`, `R000089`, and `R000098`.

It also resolved all 16 Stage 7 `WITHIN_STAGE` routes:

`R000104`, `R000106..R000113`, `R000115`, `R000116`, `R000118`, `R000119`,
`R000135`, `R000139`, and `R000164`.

The remaining 61 Stage 7 `CROSS_RANGE` routes stay pending for future
canonical target stages. Together with prior obligations, the live route
ledger contains 37 resolved and 141 pending routes. No future range was
pre-reviewed to close them.

## Source Boundaries And Defects

- `U000343`/`A001501` contain only a severely cropped fragment of a Rule 30
  label; no evolution or transition mechanics were recovered from it.
- `U000374`/`A001552` retain a trajectory whose identifying label is clipped;
  nearby reviewed text recovers the identity without treating the pixels as
  hidden mechanics.
- `A000407` is an assigned unreferenced orphaned text strip duplicating the
  opening of `U005406`, not a figure.
- `A000422` is too degraded to establish its intended similarity geometry
  independently.
- `A000413..A000417` establish construction-bearing localized-background
  variants, but not an unambiguous complete background word, phase, head
  position, or head state.

The source statuses are therefore 645 clear, 6 ambiguous, and 2 defective
reading units, plus 124 clear, 5 ambiguous, and 4 defective assets. No source
text or image was repaired, and no missing mechanics was inferred.

## Detailed Implementation Plan

1. Build and verify one sealed epoch-1 bundle for both paired paths.
2. Prepare the exact nonsemantic 653-row/133-asset worksheet.
3. Read every source unit in canonical order and record only source-grounded
   dispositions, evidence, candidates, routes, and source boundaries.
4. Screen all 133 images in context, escalating construction-bearing,
   text-bearing, ambiguous, or caption-incomplete assets to original
   resolution.
5. Complete every provisional fingerprint and missing-mechanics boundary.
6. Verify the completed bundle, preview canonical allocations, and apply one
   atomic `INITIAL` transaction.
7. Resolve reviewed incoming and `WITHIN_STAGE` routes without pre-reading
   future ranges.
8. Run frozen candidate-derived and guardrail LOCAL queries, disposition every
   hit, and repeat to a zero-delta round.
9. Run ordinary/optimized Stage 7 gates, mutation/self-tests, regressions,
   corpus/hash checks, byte-compilation, and scope checks.
10. Record exact results here and advance the plan to Stage 8.

## No-Cheating Checks

- Sequential reading and visual inspection precede search.
- The worksheet has no semantic defaults.
- All immutable row/asset identities remain bundle-bound.
- Worker-local IDs follow frozen discovery traversal and are allocated
  globally only by the coordinator.
- Blind outputs remain free of reconciliation and architecture conclusions.
- LOCAL scope union must equal exactly the two reviewed paths.
- Green tooling does not substitute for source reading.

## Completion Requirements

- All 653 source units are individually reviewed: **met**.
- All 133 images are screened at the required depth: **met**.
- Every candidate has complete source-limited provenance and fingerprint:
  **met**.
- Every relevant reference is resolved locally or queued with typed scope:
  **met**.
- Every LOCAL hit has a final blind disposition and exact two-path scope:
  **met**.
- No Stage 7 `WITHIN_STAGE` route remains pending: **met**.
- `validate_audit.py --require-stage 7` passes in ordinary and optimized
  Python: **met**.

## Stage Results

The verified constituent outputs and combined Stage 7 output have SHA-256
digests:

- main R5: `ed59ddd416977b3a65f2586f478b8d7fa55611202331fc51497978b2c458ceb8`;
- Notes R4:
  `8b34e7d2b09dc6be9b040ba30240e5602f8e4a2798b270bb7ce297f9e425da6a`;
- combined final2:
  `4a9783ed23fcafc3b27466a558864119c1f2f39b6d8d2b0ac35b361829b6cfd1`.

The coordinator allocated `B0092..B0320`, `E000264..E000746`,
`G000264..G000746`, and `R000102..R000178` in `V000012`. The governed route
proposal SHA-256 is
`76fd0508e4afa6db53954bba52377b84742710ecbc6dab99d10ebe0c097a2468`;
its 27 updates became `V000013`. `S007` and `S008` became `V000014` and
`V000015`.

The Stage 7 gate passes in ordinary and optimized Python:

```text
python3 goal-4/tools/validate_audit.py --goal-dir goal-4 --require-stage 7
python3 -O goal-4/tools/validate_audit.py --goal-dir goal-4 --require-stage 7
  validated blind audit harness: units=14311 reviewed=1441 candidates=320
  routes=178 assets=1607 screened=216 rounds=8
```

The full Goal 4 regression suite passes all 100 tests in ordinary and
optimized Python. The first optimized run exposed one relocation-fixture
failure: a hard link crossed filesystems and raised `EXDEV`. The fixture now
falls back from `os.link` to `shutil.copy2` only for `errno.EXDEV`, preserving
all other errors. The corrected optimized aggregate passes all 100 tests with
only pytest's expected warning about disabled test-module assertions under
`-O`.

There are no pending Stage 7 `WITHIN_STAGE` routes, no undispositioned LOCAL
hits, and no semantic discovery on the terminal rerun. The blind artifacts
contain no T-ID, catalog, semantic-family, API-fit, or implementation
conclusion. Stage 8 may begin from this verified ledger state without
pre-reading its source.
