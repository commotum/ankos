# 6-CH02-EXPERIMENT

Status: **IN PROGRESS**.

## Current Facts

- Stages 1–5 are complete.
- The valid live state contains 299 reviewed source units, 5 screened images,
  5 active blind candidates, 11 pending cross-range routes, and 4 closed
  LOCAL rounds.
- Stage 6 owns exactly two canonical documents in manifest order:
  - `CHAPTERS/02-The-Crucial-Experiment.md`;
  - `BACK-MATTER/NOTES/02-The-Crucial-Experiment-Notes.md`.
- The paired paths contain 489 deterministic source units:
  `U000174..U000305` and `U004962..U005318`.
- They own 78 physical images: 22 referenced from the main chapter and 56
  referenced from the Notes.
- Stage 6 will be one epoch-1 `INITIAL` transaction followed by one or more
  epoch-1 `LOCAL` search transactions and any applicable route transactions.

## Updated Assumptions

- Chapter 2's elementary-rule examples may be presets, seed restrictions,
  behavior evidence, controls, or representations; familiar rule numbers do
  not decide those roles in advance.
- A class-of-behavior label is not automatically a native construction, while
  a materially delimited rule, seed class, schedule, or finite specification
  can be a coverage candidate.
- Figure-only information is evidence only after contextual visual screening;
  no transition lookup, timing, boundary, or color value is inferred from
  pixels that do not establish it.
- Stage 4/5 candidates and vocabulary guide local recall only after complete
  sequential reading and visual screening.

## Big Picture Objective

Blindly audit Chapter 2 and its Notes for every construction-bearing system,
formal object, preset, restriction, input/seed class, representation,
application, and source defect while keeping native mechanics distinct from
behavior classes and observations.

## Allowed Inputs And Scope

Allowed:

- Goal 4 plan, loop, guardrails, corpus map, harness, and current blind
  ledgers;
- only the two assigned Chapter 2 paths, their 489 source units, and their 78
  owned images;
- deterministic LOCAL searches over exactly those two paths after sequential
  review.

Forbidden:

- current catalog mappings or identifiers;
- prior type conclusions;
- API-fit, executor, or implementation-support conclusions;
- unassigned Book ranges except through explicit pending routes.

All writes remain under `goal-4/`; sealed temporary review and visual-screening
material may be created under `/tmp`.

## Source Coverage

Starting coverage:

| Obligation | Assigned | Completed |
|---|---:|---:|
| Source units | 489 | 0 |
| Physical images | 78 | 0 |
| LOCAL search scopes | 2 paths | 0 |
| Search hits | 0 | 0 |
| Within-stage routes | 0 | 0 |
| Cross-range routes opened | 0 | 0 |

Every unit and image requires an individual disposition, source/evidence
status, concise evidence statement, epoch/stage/reviewer metadata, and exact
candidate/route links.

## Candidate Changes

No Stage 6 candidate has been proposed. Any discovery receives the next
worker-local `W####` identity in frozen traversal and a global `B####`
identity only through coordinator preview/apply.

## Search And Evidence Log

No Stage 6 query has run. LOCAL search begins only after all 489 units are
reviewed and all 78 images are screened.

## Detailed Implementation Plan

1. Build and verify one sealed epoch-1 bundle for both paired paths.
2. Prepare the exact nonsemantic 489-row/78-asset worksheet.
3. Read every source unit in canonical order and record only source-grounded
   dispositions, evidence, candidates, and routes.
4. Screen all 78 images in context, escalating construction-bearing,
   text-bearing, ambiguous, or caption-incomplete assets to original
   resolution.
5. Complete every provisional fingerprint and missing-mechanics boundary.
6. Verify the completed bundle, preview canonical allocations, and apply one
   atomic `INITIAL` transaction.
7. Run frozen candidate-derived and guardrail LOCAL queries over exactly both
   reviewed paths, disposition every hit, and repeat to a zero-delta round.
8. Resolve all Stage 6 `WITHIN_STAGE` routes and retain legitimate
   `CROSS_RANGE` routes for their owning review stages.
9. Run ordinary/optimized Stage 6 gates, mutation/self-tests, relevant
   regressions, corpus/hash checks, and diff/scope checks.
10. Record exact results here and update `0-plan.md` to the Stage 7 handoff.

## No-Cheating Checks

- Sequential reading and visual inspection precede search.
- The worksheet has no semantic defaults.
- All immutable row/asset identities remain bundle-bound.
- Worker-local IDs follow frozen discovery traversal and are allocated
  globally only by the coordinator.
- Blind outputs remain free of reconciliation and architecture conclusions.
- LOCAL scope union must equal exactly the two reviewed paths.
- Green tooling does not substitute for source reading.

## Completion Requirements

- All 489 source units are individually reviewed.
- All 78 images are screened at the required depth.
- Every candidate has complete source-limited provenance and fingerprint.
- Every relevant reference is resolved locally or queued with typed scope.
- Every LOCAL hit has a final blind disposition and exact two-path scope.
- No Stage 6 `WITHIN_STAGE` route remains pending.
- `validate_audit.py --require-stage 6` passes in ordinary and optimized
  Python.

## Stage Results

In progress. Exact transaction IDs, candidates, evidence, routes, searches,
decisions, and verification results will be recorded after execution.
